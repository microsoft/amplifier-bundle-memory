# Conformance ledger — amplifier-bundle-memory. Top-level YAML list; row AMM-000 is SYNC.
# Re-pinned to the v2 contracts on 2026-09-06 (ratified 'lgtm, do it'); suggestions.v1 pinned 2026-09-06 (ratified 'ratified').
# session.v3 locked and store.v2 amended 2026-09-07 (ratified 'ratified'); both re-pinned, rows AMM-010/012/014/015 -> GAP, AMM-041/042 seeded.
# store.v3, session.v4, cli.v3, suggestions.v2 locked and VISION.v2 revised 2026-09-07 (ratified 'ratified'); all five pinned,
# 12 changed clauses -> GAP with the gap named, 30 carried, AMM-043/044/045 seeded.
- id: AMM-000
  title: SYNC — pinned contract files, and the vision they derive from
  contract:
    files:
    - path: contracts/store.v3.md
      sha256: ff88965e5cc46119290cc69da8a56b2c11643532b6fdd3e31df3dcdd53d293a1
    - path: contracts/session.v4.md
      sha256: 995d3fc2de79723b8e1b19eda467f94a5426a8827910a7ff915806bf1526b12d
    - path: contracts/cli.v3.md
      sha256: 5d0250e83ebe50146bdad77f0074c2f19aa1b907fe74ac1fb6d70c7c6aaf98b1
    - path: contracts/suggestions.v2.md
      sha256: 88a2ef041bfcd30352072fe2287eb1a05afe8fef9e0c6176894ac42d9173471a
    - path: docs/VISION.v2.md
      sha256: 34366ada30e1ed5c964391b9c81c5e5a78bdd9e0b99c6ffc5d0296a61e5448c6
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: ledger/checks/test_row_amm_000_sync.py::test_row_amm_000_sync
  work: amplifier_bundle_memory-3q5
  notes: 'Probe written by lane A with the first conformance kit and passing: it re-hashes the three pinned
    files and fails loud on any drift. A hash mismatch still means a full-ledger re-review, never a silent
    bump.

    2026-09-06 21:00Z — re-pinned to the v2 files on ratification (''lgtm, do it''); every row below re-reviewed
    against v2 in the same edit.

    2026-09-06 23:05Z — suggestions.v1 locked (ratified ''ratified''); pinned as the fourth file. Rows
    AMM-027..036 seeded below at Not yet.

    2026-09-07 15:05Z — session.v3 locked (FROZEN 2026-09-07) and store.v2 amended in place (both ratified
    ''ratified''); re-pinned. Rows AMM-010/012/014/015 re-reviewed to GAP against the v3 text; AMM-041/042
    seeded.

    2026-09-07 16:40Z — session.v3 and cli.v2 amended in place (''ok, do it''); both re-pinned. AMM-015
    → GAP (paged markdown list/review); cli Core 8 row → GAP (init installs the timer).


    2026-09-07 19:10Z — store.v3, session.v4, cli.v3 and suggestions.v2 locked (FROZEN 2026-09-07, steward''s
    word ''ratified''), superseding store.v2 / session.v3 / cli.v2 / suggestions.v1; the vision is now
    docs/VISION.v2.md. All five re-pinned here and the whole ledger re-reviewed against the new bytes
    in this one edit — never a silent hash bump. VISION.v2 is pinned for the first time: it is the document
    2026-09-07 — the four contracts amended in place on the steward''s word "Let''s fix those wrinkles." (commit c9ef8a5): a header, cross-references to superseded versions, cli §8''s role-not-provider sentence, two shell-edit examples naming <instance>/MEMORY.md. No clause''s meaning changed, so no row''s verdict moves; the four hashes are re-pinned here for that reason and no other.
    2026-09-07 — cli.v3 amended in place a second time on the steward''s word "Take care of that residue." (Conformance bullet about init on a temp store re-worded to match §8). No clause changed, no verdict moves; cli.v3''s hash re-pinned for that reason only.
    the contracts are derived from, so a silent change to it is exactly the drift this row exists to catch.
    It is pinned as a GOVERNING DOCUMENT, not as a contract with rows of its own — whether the vision
    earns rows is the steward''s call, not this pass''s. Twelve clauses moved and their rows earn GAP
    today (store §1 §2 §7; session §2; cli Core 1 §5 §6 §8; suggestions Core 2 3 8 9); thirty clauses
    are unchanged and their verdicts are carried; AMM-043/044/045 seeded for store §11 and session §12/§13.
    Two quotes that verified against NO version of their contract (AMM-011, AMM-017) were re-anchored
    to the bytes in the same pass.

    '
- id: AMM-001
  title: store.v3 Core 1 — Location — an instance.
  contract:
    file: contracts/store.v3.md
    clause: Core 1
    quote: 'Location — an instance.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_1
  work: amplifier_bundle_memory-6x6
  notes: 'Kept 2026-09-06 by lane A; broken the same day by the steward''s real session (feedback drop
    2026-09-06-kicked-the-tires-transcript.md — three concurrent saves clobbered MEMORY.md and one reported
    success for a line that was never committed); kept again 2026-09-06 by lane F, which put every mutating
    path under one exclusive fcntl.flock and made the writer re-read the committed tree before reporting
    anything. probe_core_1 now calls probe_concurrency: 8 concurrent saves leave 8 well-formed lines,
    ids m-001..m-008 with no gap or duplicate, 8 save commits, a clean tree, and every returned id present
    in `git show HEAD:MEMORY.md`. It discriminates — with the lock removed the same 8 calls leave 1 line
    and 0 successes. The probe also still asserts the store is a git repo at $AMPLIFIER_MEMORY_HOME and
    that init/save/forget are exactly 1/2/3 commits with a clean tree. Caveat recorded as a residual:
    log_usage also commits, so a per-request usage log would commit per request.

    2026-09-06 21:00Z — reseeded against v2: GAP. store.v2 §1: usage appends still commit (one `usage:
    loaded` per session). Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). store.v2 §1''s exception is built: log_usage appends
    without a commit and usage.jsonl is untracked (init writes the store''s .gitignore). probe_core_1
    now calls probe_reading_leaves_no_commit as well as probe_concurrency: three loads on a fresh store
    leave 0 commits and 3 events, and a PRE-v2 store that tracks usage.jsonl migrates in exactly one visible
    commit (''store: stop tracking usage.jsonl (store.v2 §1)'') and never again, losing no line. init/save/edit/forget
    are exactly 1/2/3/4 commits. Discriminating: restore the commit inside log_usage and the count rises
    by three. The residual recorded against this row (a per-request usage log would commit per request)
    is closed by the same change. tests/test_store.py::test_a_session_that_only_loads_leaves_no_commit_behind
    and ::test_usage_jsonl_is_untracked_and_a_pre_v2_store_migrates_once carry it in the suite.


    2026-09-07 19:10Z — GAP against store.v3. §1 is now an INSTANCE: `Resolved in order: an explicit `home`
    from the caller (the modules'' mount-plan `config: home:`, or the CLI''s `--home`), else `$AMPLIFIER_MEMORY_HOME`,
    else the default `~/.amplifier-memory`.` and `**Migration:** when the default does not exist and `~/.amplifier/memory`
    does, the older path is the default; `init` offers to move it, and says so.` Today: store_home() (src/amplifier_memory/store.py:461-468)
    honours an explicit `home=` and `$AMPLIFIER_MEMORY_HOME` but its default is `~/.amplifier/memory`,
    not `~/.amplifier-memory`; there is no `--home` on the group or on any verb (cli.py carries no such
    option), no mount-plan `home:` in either module (the hook reads only `priority`, hooks_memory_inject/__init__.py:296;
    the tool reads nothing, tool_memory/__init__.py:560), and no migration offer anywhere. The one-commit-per-mutation
    half of §1 and its usage.jsonl exception are unchanged and still Kept — what this row now measures
    red is the instance half. Lane U (6x6, held).

    2026-09-07 19:30Z — CONFORMS by lane U (6x6), merged d324c6c; the manager re-ran conformance/store/run.py::probe_core_1 on main: Kept — instance resolution caller home → $AMPLIFIER_MEMORY_HOME → ~/.amplifier-memory, legacy ~/.amplifier/memory honoured when the default is absent.

    '
  previous_disposition: CONFORMS
- id: AMM-002
  title: store.v3 Core 2 — Fixed layout.
  contract:
    file: contracts/store.v3.md
    clause: Core 2
    quote: 'Fixed layout.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_2
  work: amplifier_bundle_memory-6x6
  notes: 'Kept 2026-09-06 by lane A. The probe asserts the on-disk set is exactly MEMORY.md, declined.md,
    inbox.md, topics/, usage.jsonl, and that a stray notes.txt carrying an m-NNN line is not read as memory.
    topics/.gitkeep is a git mechanism, not memory.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — GAP against store.v3. §2''s layout gains two lines: `config.yaml        this instance''s
    configuration (§11) — NOT memory` and `sessions.jsonl     one line per session seen — NOT memory`,
    and `config.yaml` holds what shipped as `~/.amplifier/memory-config.toml` — `[llm.judge]` becomes
    `llm: judge:`, plus `enabled: true|false` (§11); the `.toml` is retired.` Today: LAYOUT_FILES = (''MEMORY.md'',
    ''declined.md'', ''inbox.md'', ''usage.jsonl'') (store.py:98) plus topics/; `grep -rn ''config.yaml|sessions.jsonl''
    src/ modules/` returns zero hits, so neither file is written or read anywhere; the knob still lives
    in `${AMPLIFIER_MEMORY_CONFIG:-~/.amplifier/memory-config.toml}` (llm_config.py:14, 51-57), the file
    §2 now retires. Lane U (6x6, held).

    2026-09-07 19:30Z — CONFORMS by lane U (6x6), merged d324c6c; the manager re-ran conformance/store/run.py::probe_core_2 on main: Kept — config.yaml (enabled, llm: judge:) and sessions.jsonl inside the instance, both plumbing; memory-config.toml retired from the library.

    '
  previous_disposition: CONFORMS
- id: AMM-003
  title: store.v3 Core 4 — The cap is enforced by the writer, not by advice.
  contract:
    file: contracts/store.v3.md
    clause: Core 4
    quote: 'The cap is enforced by the writer, not by advice.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_4
  work: amplifier_bundle_memory-3q5
  notes: 'Kept 2026-09-06 by lane A. Discriminating pair: the 200th MEMORY.md line saves, the 201st raises
    CapExceeded naming 200, ''topic file'' and ''/forget'', and the refused line is not written. Headings
    and blank lines count toward the cap.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-004
  title: store.v3 Core 5 — Topic files
  contract:
    file: contracts/store.v3.md
    clause: Core 5
    quote: 'Topic files

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_5
  work: amplifier_bundle_memory-3q5
  notes: 'Kept 2026-09-06 by lane A. The probe asserts a new topic file must begin with a one-line purpose,
    the 151st line of a topic file is refused, and the 51st topic file is refused and never created.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-005
  title: store.v3 Core 6 — Provenance lives in git.
  contract:
    file: contracts/store.v3.md
    clause: Core 6
    quote: 'Provenance lives in git.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_6
  work: amplifier_bundle_memory-3q5
  notes: 'Kept 2026-09-06 by lane A. Every writer commit carries [m-NNN], the text, quote: "<verbatim>",
    session: and writer:; why(id) parses all five back from git log --grep. No separate provenance store
    exists.

    2026-09-06 21:00Z — reseeded against v2: GAP. store.v2 §6: `was:` on edits and the `forgot` subject
    marker do not exist; `action` does. Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). store.v2 §6 is complete: a forget''s subject reads `forgot
    [m-NNN] …` and an edit carries `was: "<previous text>"` with `action: edit`, keeping the id. probe_core_6
    asserts all three moments on one memory and reads them back through why(); it discriminates — drop
    the marker and `git log --oneline` shows a save and its forget as identical lines, which is the defect
    the clause was written for (Dana persona run). Also tests/test_store.py::test_edit_keeps_the_id_and_records_what_it_replaced
    and ::test_forget_commits_a_forgot_subject.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: CONFORMS
- id: AMM-006
  title: store.v3 Core 7 — declined.md
  contract:
    file: contracts/store.v3.md
    clause: Core 7
    quote: 'declined.md

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_7
  work: amplifier_bundle_memory-6x6, amplifier_bundle_memory-5eb
  notes: 'CONFORMS 2026-09-06 by lane P: Phase 2 built the decline path this row was waiting for. declined.md
    is append-only, `- <YYYY-MM-DD> <text>`, written in the same commit that removes the item from inbox.md;
    the exact-match block is code, not prompt advice (whitespace-normalised so an editor''s stray space
    cannot resurrect a decline), and the same list is handed to the §3 prompt. probe_core_7 declines a
    proposal and runs again over the same transcript: it is not re-proposed, and removing the check returns
    it. Also tests/test_inbox.py::test_decline_appends_the_dated_line_and_is_never_re_proposed. RESIDUAL:
    conformance/store/run.py::probe_core_7 still prints "Core 7 - Can''''t check" from Phase 1 and is
    now stale; that kit is not this lane''''s to edit.


    2026-09-07 19:10Z — GAP against store.v3. §7 now reads `an append-only list of declined suggestions,
    one per line: `- <YYYY-MM-DD> <text>  quote: "<quote>"` — the verbatim human quote after the text`
    and `matched exactly by code on **text OR quote**, so a declined suggestion returning paraphrased
    is blocked.` Today: decline() appends `- <YYYY-MM-DD> <text>` with no quote (inbox.py:105, 698-727)
    and both is_declined() and the append() dedupe key on the text alone (inbox.py:425-432, 571-583) —
    pinned in the suite as tests/test_inbox.py::test_declined_dedupe_is_text_only_today, and filed as
    the live defect amplifier_bundle_memory-5eb. Old two-field lines staying readable is the one half
    already true. Lane U (6x6, held).

    2026-09-07 19:30Z — CONFORMS by lane U (6x6), merged d324c6c; the manager re-ran conformance/store/run.py::probe_core_7 on main: Kept — declined.md lines carry the verbatim quote; old two-field lines still parse; inbox.append drops a candidate whose quote matches a declined quote.

    '
  previous_disposition: CONFORMS
- id: AMM-007
  title: store.v3 Core 8 — usage.jsonl
  contract:
    file: contracts/store.v3.md
    clause: Core 8
    quote: 'usage.jsonl

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_8
  work: amplifier_bundle_memory-3q5
  notes: 'Kept 2026-09-06 by lane A. log_usage appends {ts,event,target,session_id} and truncates to the
    last 90 days on each write; a seeded 91-day-old entry is dropped and nothing else is.

    2026-09-06 21:00Z — reseeded against v2: GAP. store.v2 §8: no `cited` event; usage.jsonl is still
    committed. Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). store.v2 §8 is complete: `cited` joins loaded/read (record_citation(memory_id,
    session_id) appends {ts, event: cited, target: m-NNN, session_id}), the 90-day truncation is unchanged,
    and usage.jsonl is never committed. probe_core_8 asserts the four fields, the truncation, the cited
    event, and that a cited event REFUSES a file target; the no-commit half is evidenced by probe_core_1''s
    §1-exception run. tests/test_store.py::test_a_citation_is_recorded_against_a_memory_id_and_never_a_file.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: CONFORMS
- id: AMM-008
  title: store.v3 Core 9 — Two writers, one path.
  contract:
    file: contracts/store.v3.md
    clause: Core 9
    quote: 'Two writers, one path.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_9
  work: amplifier_bundle_memory-cop
  notes: 'Kept 2026-09-06 by lane A; broken the same day by the steward''s real session (two parallel
    forgets: one raised a raw `Command ''[''git'', …]''` at the human although both removals had landed);
    kept again 2026-09-06 by lane F. probe_core_9 still asserts a hand-edited line is read back, never
    clobbered, and its id never reissued, and now also interleaves: a hand commit taken under the store
    lock during 4 concurrent saves leaves every writer line committed, the hand line intact, all lines
    well-formed, and the next id past the hand-written one. Named limit, not silently assumed away: the
    lock protects writers from each other and any hand edit made through this library or the CLI; a human
    editing MEMORY.md in an editor takes no lock, so an editor save landing inside a writer''s read-modify-write
    is still last-writer-wins — `doctor` now names that damage and `doctor --repair` restores it.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-009
  title: store.v3 Core 10 — Bounded by construction.
  contract:
    file: contracts/store.v3.md
    clause: Core 10
    quote: 'Bounded by construction.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_10
  work: amplifier_bundle_memory-3q5
  notes: 'Kept 2026-09-06 by lane A for Phase 1: MEMORY.md 200, topic 150, topics 50 and usage 90d all
    refuse or truncate under probe. The inbox 30-day expiry is suggestions.v1 (DRAFT), outside Phase 1
    and outside this probe.

    2026-09-06 21:00Z — reseeded against v2: GAP. store.v2 §10: history bound depends on §1''s exception,
    not yet built. Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). store.v2 §10''s history bound now rests on a built §1
    exception. probe_core_10 adds the discriminating pair on one store: ten reads (five loads, five citations)
    grow the history by 0 commits, while one save grows it by exactly 1 — git history grows only with
    changes a human made or approved. The four size caps (MEMORY.md 200, topic 150, topics 50, usage 90d)
    are unchanged; the inbox 30-day expiry is still suggestions.v1 (DRAFT).


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: CONFORMS
- id: AMM-010
  title: session.v4 Core 1 — Loaded in every request.
  contract:
    file: contracts/session.v4.md
    clause: Core 1
    quote: 'Loaded in every request.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_010
  work: amplifier_bundle_memory-zqi
  notes: 'Kept 2026-09-06 by modules/hooks-memory-inject: one handler on provider:request returns inject_context
    / role=system / ephemeral=True carrying the §1 framing sentence as the first line inside the envelope,
    MEMORY.md verbatim, and no topic bodies; two instances over the same store are byte-identical and
    carry no timestamp or session id. Probe lives in the module suite: `cd modules/hooks-memory-inject
    && uv run --offline pytest -q -k test_row_amm_010` (18 passed 2026-09-06). It is NOT yet discovered
    by the repo-root `uv run pytest` — wiring the module suites into the root gate is a residual for the
    manager. conformance/session/inject/run.py additionally re-extracts the framing sentence from the
    locked contract and compares it byte-for-byte with the module constant, so the two cannot drift apart
    silently. Registration on provider:request (not session:start, not context:post_compact) is what makes
    "every request, including after a compaction" true by construction — no first-turn flag exists to
    go stale.

    2026-09-06 21:00Z — reseeded against v2: GAP. session.v2 §1: the injected block still carries the
    announce instruction. Lane I.

    Lane I 2026-09-06 — CONFORMS against v2. The announce instruction is gone from the block, and so are
    the counts: render_block() now takes only the memory text, and ANNOUNCE_PREFIX / announce_instruction()
    no longer exist. The framing sentence is v2 §1''s (with `/edit <id> <text>`) and is still re-extracted
    from the locked contract and compared byte-for-byte by conformance/session/inject/run.py, which prints
    `Core 1 — Kept` naming the sha of the block two hook instances produced over the same store (identical,
    363 chars). Probes: `cd modules/hooks-memory-inject && uv run pytest -q -k "test_row_amm_010 or no_announce_instruction
    or byte_identical_across_instances_by_sha"` (36 passed in the module suite 2026-09-06). Byte-identity
    is now true by construction rather than by care — the block is a pure function of MEMORY.md — which
    is what retires the mid-session re-fire the steward saw after a save.


    2026-09-07 15:05Z — GAP against session.v3: §1 framing now ends `To change one: `/remember <text>`
    or `/memory`.`; hooks-memory-inject/__init__.py:76 still names /forget /edit /remember. Lane 13-C.

    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lane 13-C. hooks-memory-inject
    constant is v3 §1; conformance/session/inject/run.py re-extracts §1 from contracts/session.v3.md and
    prints `Core 1 — Kept — framing sentence byte-identical … two instances byte-identical (337 chars)`.
    Module suite 51 passed.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-011
  title: session.v4 Core 2 — Announce the load, once, in code.
  contract:
    file: contracts/session.v4.md
    clause: Core 2
    quote: 'Announce the load, once, in code.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/inject/run.py::check_core_2
  work: amplifier_bundle_memory-8o4
  notes: 'Still Can''t check after 2026-09-06, deliberately. The injected block now ends with a static
    instruction asking for the one-time announce, and both variants are verified in-process (populated:
    ''Loaded N memories (M topics available).''; empty: ''No memories yet — /remember <text> to add one.'')
    by conformance/session/inject/run.py and the module suite. Whether the assistant actually *says* it
    once cannot be observed in this process; only the real-host smoke can close this row. Seen Kept in
    a real session on this device 2026-09-06 (lane E): the empty-store variant in tests/smoke/evidence/session-A0-empty-announce.txt
    ("No memories yet"), the populated variant in session-C-load.txt ("Loaded 1 memories (0 topics available)."
    with the id cited). Row stays NOT-ASSERTABLE: a smoke run is evidence for a date, not an assertion
    that re-runs with the suite. One qualification, measured the same day: the announce is an instruction
    inside the injected block, so a human instruction constraining the reply can outrank it — with "reply
    with exactly the word ok" it appeared in 1 of 2 runs (session-A-task-scoped.txt, pair-in-one-turn-run1.txt).

    2026-09-06 21:00Z — reseeded against v2: GAP. session.v2 §2: announce is model-instructed, not rendered
    via user_message; singular/plural, compaction and empty-store texts differ from v2. Lane I.

    Lane I 2026-09-06 — CONFORMS against v2, and no longer NOT-ASSERTABLE: the clause is now asserted
    at the layer it names. The line is rendered by the hook, in code, so it can be captured and diffed
    — which a model''s willingness could not be. Device-checked in a real PTY session on this host (amplifier
    2026.09.06-be25afd, core 1.6.1) against a temp store of 3 memories: tests/smoke/evidence/announce-rendered-turn1.txt
    carries `[amplifier-memory] 3 memories loaded. /memory to see them.`, announce-rendered-turn2.txt
    shows it absent on the second turn, and announce-rendered-constraint.txt shows it still present when
    the human''s FIRST turn is "Reply with exactly: ok" — the arm that suppressed the v1 announce 1-of-2
    under lane E. In-process probes: conformance/session/inject/run.py prints `Core 2 — Kept` (4 requests
    + one context:compaction → exactly 2 rendered lines; every variant byte-compared to modules/hooks-memory-inject/tests/fixtures/announce-lines.txt,
    whose 5 contract-origin lines are checked against the locked §2 and whose 2 derived lines are reported
    as derived); module suite `cd modules/hooks-memory-inject && uv run pytest -q -k "announce or amm_011"`
    (36 passed 2026-09-06).

    Mechanism, measured and worth carrying: `user_message` alone does NOT reach the human here. The kernel
    aggregates every handler''s HookResult for an event, and as soon as ANY handler returns action=inject_context
    the aggregate''s user_message is None — seven combinations measured with a spy DisplaySystem, kept
    as test_kernel_aggregation_drops_user_message_when_a_hook_injects. This hook injects by definition.
    So it calls coordinator.display_system.show_message directly — the same DisplaySystem process_hook_result
    would call, one aggregation earlier — and falls back to user_message only where there is no display
    system, so exactly one line is produced, never two. Evidence and the live session that rendered nothing:
    tests/smoke/evidence/announce-rendered-why-not-user_message.txt. Same bug swallows §10''s failure
    line, which has therefore never been displayable either: filed as amplifier_bundle_memory-87j, not
    fixed here.

    Compaction signal: context-simple''s own `context:compaction` event (amplifier_module_context_simple/__init__.py:1753-1755).
    `context:post_compact` is declared and never emitted, and that module''s compaction is ephemeral —
    the readable message list never shrinks — so this is the only public signal in this build. The handler
    arms a flag; the line renders on the next provider:request. The post-compaction line is device-checked
    only in-process (a real compaction needs a 200k-token session): session.v2 §2 — the compaction VARIANT
    is Can''t check on a real terminal in this lane, because nothing in a short session compacts; what
    is checked is that the hook renders it when the event fires.


    2026-09-07 19:10Z — GAP against session.v4, and a ledger-integrity correction in the same edit. The
    quote this row carried, `Announce the load, once.`, verified against NEITHER session.v3 nor session.v4
    — the lead has read `Announce the load, once, in code.` since v2 and the row''s anchor was never moved
    with it. Re-anchored to the bytes; no verdict rests on the correction. The v4 change this row is red
    for: `When the session''s instance (§12) is not the default one, the line names it — `3 memories loaded
    from ~/.amplifier-agent/memory. /memory to see them.` — so a human never has to guess which store
    answered. The default instance is never named: the common line stays byte-identical to today''s.`
    Today the hook has no instance concept at all (no `home:` in its config, hooks_memory_inject/__init__.py:295-296),
    so it can never name one; the default-instance half is byte-identical by construction and still Kept.
    Lane V (8o4).

    2026-09-07 20:15Z — CONFORMS by lane V (8o4), merged b9e9eef; the reconciler re-ran
    conformance/session/inject/run.py::check_core_2 on merged main (6a64d7e) in the module venv:
    Kept — "6 lines appear verbatim in contracts/session.v4.md §2; 5 derived from §2''s rule, not
    quoted from it […] named_instance: ''3 memories loaded from ~/.amplifier-agent/memory. /memory
    to see them.''; named_instance_with_topics: ''3 memories loaded from ~/.amplifier-agent/memory,
    2 topics. /memory to see them.''; named_instance_singular: ''1 memory loaded from
    ~/.amplifier-agent/memory.''; named_instance_compacted: ''context compacted. 3 memories still
    loaded from ~/.amplifier-agent/memory.''; mount plan `home:` names the instance (''3 memories
    loaded from /tmp/tmpebnkxmcj/named-instance. /memory to see them.''); the SAME store with no
    `home:` still renders today''s line (''3 memories loaded. /memory to see them.'') — §12''s
    "behaves exactly as today"" (excerpt; the full line is in the run this note records). Both
    halves of the v4 change are measured: the non-default instance is named, and the default line is
    byte-identical to today''s. The assertion moves off the module test name `test_row_amm_011` onto
    the kit probe, which is what re-runs here. Carried residual, unchanged by this landing and not
    silently dropped: the post-compaction VARIANT is still Can''t check on a real terminal (nothing
    in a short session compacts); what the kit checks is that the hook renders it the moment
    context:compaction fires.

    '
  previous_disposition: GAP
- id: AMM-012
  title: session.v4 Core 3 — Save on correction, in the same turn.
  contract:
    file: contracts/session.v4.md
    clause: Core 3
    quote: 'Save on correction, in the same turn.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/tool/run.py
  justification: '§3 has two halves and they are reported separately. The RECEIPT half is code in this
    bundle and is asserted byte for byte by conformance/session/tool/run.py (`Core 3 (receipt) — Kept`)
    plus the module suite. The CALLING half — whether the assistant calls the tool in the correcting turn
    — is model behaviour and is reported "session.v2 Core 3 — Can''t check in this lane because whether
    the assistant calls the tool in the correcting turn is model behaviour"; its evidence is the real-host
    smoke (lane E, 2026-09-06), taken under the v1 receipt and owed one re-run under v2.

    '
  work: amplifier_bundle_memory-yvu
  notes: 'Seen Kept in a real session on this device 2026-09-06 (lane E): tests/smoke/evidence/session-B-correction.txt
    — the human states "For future reference: never use tabs in YAML files you write for me; always two-space
    indentation." and within that turn the memory tool saves m-001, MEMORY.md carries one line, and the
    commit carries quote: "never use tabs in YAML files you write for me; always two-space indentation".
    Row stays NOT-ASSERTABLE. Two qualifications, both measured: (1) the save sentence reached the transcript
    as the tool''s own result line, while the assistant''s closing line was the §2 load announce; (2)
    when the correction and a task-scoped instruction share ONE turn, the model obeys the literal reply
    constraint and skips the save — reproduced 2/2 (pair-in-one-turn-run1.txt, pair-in-one-turn-run2.txt)
    against 1/1 saved with the correction alone (correction-alone-control.txt). §3 says "in the same turn";
    the Conformance line says the discriminating instruction is "in the same session". The smoke follows
    the Conformance wording, one session per turn, and the one-turn result is filed as a finding rather
    than smoothed over.

    Lane H 2026-09-06 (notes only; disposition unchanged). The tool description still carries §3''s trigger
    phrasing and the announce literal byte-for-byte — the kit re-extracts both from the locked contract
    — and now also states the batch flow (drafted lines approved with one phrase, saved one per call),
    the ids rule and the no-restate rule verbatim. The receipt keeps §3''s literal as its FIRST line and
    adds provenance under it: `your words, verbatim` for writer=human, `my wording, your go-ahead: "<quote>"`
    for writer=assistant. Evidence: conformance/session/tool/receipts.py (printed) and `Core 6 — Kept`
    in run.py.

    2026-09-06 21:00Z — reseeded against v2: GAP. session.v2 §3: receipt is v1''s literal + provenance
    line, not the three-line shape (`saved m-017 — …`, text unquoted, provenance). Lane K2.

    Lane K2 2026-09-06 (session.v2). The RECEIPT half of §3 is now code, and is asserted byte for byte:
    conformance/session/tool/run.py reports `Core 3 (receipt) — Kept` — writer=human renders exactly `saved
    m-001 — /forget m-001 to undo.` / `  <text>` / `  your words, verbatim`, writer=assistant renders
    `  my wording, your go-ahead: "<quote>"`, and a batch of three renders the set ONCE, on the save that
    completes the run, with the two earlier results carrying only their own three-line receipt. Module
    probes: `cd modules/tool-memory && uv run --offline pytest -q` (47 passed 2026-09-06), specifically
    test_save_receipt_marks_the_humans_own_words, test_save_receipt_marks_the_assistants_wording_with_the_approving_quote,
    test_a_batch_of_drafted_lines_reports_itself_once_at_the_end and test_a_batch_without_batch_of_never_prints_a_running_summary.
    Every rendered receipt is scanned for a sha, a phase name, a zero-valued count and a `<placeholder>`
    (§6) and carries none. What this row does NOT assert, and no in-process kit can: that the assistant
    CALLS the tool in the correcting turn. run.py says so in the honesty form under `Core 3 (calling)`.
    That half was seen on this device in a real session 2026-09-06 (lane E, tests/smoke/evidence/session-B-correction.txt)
    under the v1 receipt; the v2 description no longer carries an announce format, so the manager owes
    ONE re-run of the real-host smoke to re-confirm the calling half under v2. Named here rather than
    implied.


    2026-09-07 15:05Z — GAP against session.v3: §3 receipt reads `saved m-017 — /memory forget m-017 to
    undo.` and the batch tail `/memory forget <id> drops one.`; the tool still renders `/forget`. Lane
    13-A.

    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lane 13-A. Receipts render
    `saved m-017 — /memory forget m-017 to undo.` and the batch tail `/memory forget <id> drops one.`;
    conformance/session/tool/run.py prints `Core 3 — Kept` against the v3 fixtures. Tool suite 70 passed.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-013
  title: session.v4 Core 4 — Do not save
  contract:
    file: contracts/session.v4.md
    clause: Core 4
    quote: 'Do not save

      '
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: 'Model behaviour (not saving task-scoped instructions). Proven only by the real-host
    smoke (lane 16w). Still NOT-ASSERTABLE after lane C, and structurally so: not saving is the ABSENCE
    of a tool call, and an absence in a fixture proves nothing about a real session. conformance/session/tool/run.py
    reports "session.v1 Core 4 — Can''t check in this lane because not saving a task-scoped instruction
    is the absence of a call" and prints the checkable remainder (the description names §4''s exclusions:
    "do step 1", "reply with exactly ok", re-derivable facts, AGENTS.md, private).

    '
  work: amplifier_bundle_memory-yvu
  notes: 'Seen Kept in a real session on this device 2026-09-06 (lane E): tests/smoke/evidence/session-A-task-scoped.txt
    — "For this task only, reply with exactly the word ok." left MEMORY.md at 0 memory lines, and the
    neutral session A0 saved nothing either. Row stays NOT-ASSERTABLE: an absence observed on one date
    is not an assertion.

    Lane H 2026-09-06 (notes only; disposition unchanged). The description still names §4''s exclusions
    (''do step 1'', ''reply with exactly ok'', re-derivable, AGENTS.md, private); the kit reports them.
    Nothing lane H changed touches whether the model refrains — still an absence, still only observable
    in a real session.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
- id: AMM-014
  title: session.v4 Core 5 — The model proposes; the writer commits.
  contract:
    file: contracts/session.v4.md
    clause: Core 5
    quote: 'The model proposes; the writer commits.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_014
  work: amplifier_bundle_memory-yvu
  notes: 'Kept 2026-09-06 by modules/tool-memory over src/amplifier_memory. The tool proposes; the library''s
    writer commits. Discriminating pair, both arms measured: a quote present in a `role: user` turn saves
    (`- [m-001] Never use emoji in commit messages.`, commit carries the verbatim quote), and the SAME
    shape of quote present only in an assistant turn is refused in one line ("the quote does not appear
    verbatim in any human turn of this session"). Exact duplicate refused; the 200-line cap refused; an
    absent store refused with `amplifier-memory init` in the message. The check itself is NOT re-implemented
    in the tool: a spy on amplifier_memory.save records human_turns arriving as `[''always squash before
    merging'']`, so the library owns §5 and the module only supplies the evidence a library cannot see
    from outside a session. Probes: `cd modules/tool-memory && uv run --offline pytest -q -k test_row_amm_014`
    (2 passed 2026-09-06; 23 in the full module suite), and `uv run --offline python ../../conformance/session/tool/run.py`
    prints `Core 5 — Kept`. As with AMM-010, the module suite is not yet discovered by the repo-root `uv
    run pytest` — wiring the module suites into the root gate remains a residual for the manager.

    Lane H 2026-09-06 (notes only; disposition unchanged — CONFORMS). The writer still owns the check:
    the module passes `human_turns` and never re-implements it, and the spy arm in run.py proves it. What
    changed is only the relay: each library refusal now reaches the human as one plain sentence rather
    than the writer''s own wording — cap (`not saved — MEMORY.md is full (200 of 200 lines). …`), duplicate
    (`already remembered as m-002 — nothing changed.`), unknown id (its forgotten date from the store''s
    own history, plus the current ids, sorted), no human words (`can''t save that one — you haven''t said
    it in your own words yet. …`), and a catch-all that names the error log it has just appended to (session.v2
    (kit re-aimed in wave 8) §10). `(committed <sha> to …)` is gone from every result. Probes unchanged
    and still green: `cd modules/tool-memory && uv run --offline pytest -q -k test_row_amm_014`; run.py
    prints `Core 5 — Kept`.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 15:05Z — GAP against session.v3: §5 cap refusal reads `/memory forget one you no longer
    need`; the writer''s refusal string in src/ and the conformance/store needle say `/forget`. Lane 13-A.

    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lane 13-A. The writer''s
    cap refusal reads `/memory forget one you no longer need`; conformance/store probe_core_4 needle updated
    in the same change; store kit 10 Kept; tool kit `Core 5 — Kept`.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-015
  title: 'session.v4 Core 6 — Commands: `/remember <text>` and `/memory <word>`'
  contract:
    file: contracts/session.v4.md
    clause: Core 6
    quote: Two, and only two, user-invocable skills ship with the bundle.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_015
  work: amplifier_bundle_memory-nyh, amplifier_bundle_memory-42s, amplifier_bundle_memory-0jk
  notes: 'Kept 2026-09-06 for the half a process can hold. The three commands exist as user-invocable,
    model-invisible skills — skills/{remember,forget,memory}/SKILL.md each carry `user-invocable: true`
    and `disable-model-invocation: true`, the keys tool-skills parses at amplifier_module_tool_skills/discovery.py:292-306
    — and each calls the `memory` tool with $ARGUMENTS. Behaviour measured through the tool: /remember
    writes exactly what was typed (writer="human", quote set to the text itself, which is what store.py
    requires) and announces `Saved memory m-001: "Always run make check before pushing." — /forget m-001
    to undo.`; /forget announces `Forgot m-001.` and the line leaves MEMORY.md; an unknown id is the one-line
    error `unknown memory id ''m-404''`; /memory prints the ids and `0 pending suggestions` (Phase 1 records
    none). What is NOT asserted by this row: that a human TYPING `/remember …` reaches the tool — that
    is the CLI''s dispatch plus model behaviour, and it belongs to the real-host smoke (lane E). Verified
    by reading the installed CLI rather than assumed: `/remember` resolves through SKILL_SHORTCUTS to
    the `load_skill` action (amplifier_app_cli/main.py:797-817), and main.py:3185-3195 turns it into a
    SYNTHETIC prompt — so the literal `/remember <text>` line is never a user message; the synthetic prompt
    is, and it carries the typed text verbatim, which is what makes the human-turn check pass. Probes:
    `cd modules/tool-memory && uv run --offline pytest -q -k test_row_amm_015` (3 passed 2026-09-06),
    and conformance/session/tool/run.py prints `Core 6 — Kept`.

    Lane H 2026-09-06 (notes only; disposition unchanged — CONFORMS). §6''s two literals are kept byte-for-byte
    and the kit now compares them against the LOCKED contract rather than retyped copies. Lines were ADDED
    under them, each answering something the 2026-09-06 transcript left unanswered: `/forget` echoes the
    removed text and `still in git: amplifier-memory why m-002` (forgetting echoed nothing at all); `/memory`
    renders `N memories` with correct singular, `-` bullets with ids first, a `, N topics` clause only
    when there are some, and ends with `edit by hand: $EDITOR <store>/MEMORY.md` (store.v1 §9''s hand-edit
    path, which no surface had ever named). `(Phase 1 records none)` and a zero topic count are gone.
    All three SKILL.md files carry the ids rule and the no-restate rule verbatim, and the kit fails if
    either is paraphrased. Probes: `uv run --offline pytest -q -k test_row_amm_015`; run.py prints `Core
    6 — Kept`.

    2026-09-06 21:00Z — reseeded against v2: GAP. session.v2 §6: no /edit; forget receipt is `Forgot m-002.`
    not `forgot m-002 — still in git: …`; /memory header/topics wording. Lane K2.

    Lane K2 2026-09-06 (session.v2). §6 now has FOUR commands and all four are asserted: skills/{remember,edit,forget,memory}/SKILL.md
    each carry `user-invocable: true`, `disable-model-invocation: true`, and — byte for byte — the ids
    rule, the no-restate rule and the cite-at-use rule; /edit is registered in behaviors/memory-session.yaml
    and bundle.md. Behaviour measured through the tool by conformance/session/tool/run.py (`Core 6 — Kept`):
    /remember writes exactly what was typed; /edit keeps the id and renders `edited m-001 — was: "…"`
    / `  now: …`; /forget renders `forgot m-002 — still in git: amplifier-memory why m-002` with the removed
    text under it; /memory renders `1 memory` for one, `3 memories` for three, `, 1 topic` only when a
    topic file exists, and ends with the hand-edit path; an unknown id is the one-line §6 refusal naming
    the date and the current ids. Module probes: `cd modules/tool-memory && uv run --offline pytest -q`
    (47 passed 2026-09-06), including test_edit_keeps_the_id_and_the_receipt_says_what_it_was and test_edit_of_an_unknown_id_is_the_one_line_refusal.
    Unchanged carve-out: that a human TYPING `/remember …` reaches the tool is the CLI dispatch plus model
    behaviour, and belongs to the real-host smoke.


    2026-09-07 15:05Z — GAP against session.v3: §6: TWO skills with first-word dispatch; bare /memory
    = 4-line overview from the status figures, suggestions first (line absent when the inbox is empty;
    `review` in the command line only while suggestions wait); list · review · forget · edit · remember
    · help; unknown word -> help table. Today: four skills, bare /memory = the listing, no overview, no
    help, no dispatch. Lane 13-A (tool `overview` op, receipts) + 13-B (skills, bundle.md, behaviors,
    READMEs).

    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lanes 13-A + 13-B. skills/
    ships exactly memory and remember (13-B, 6dd3b9d); first-word dispatch covers overview/list/review/forget/edit/save/help;
    operation=overview added to the tool (13-A, db81296) rendering §6''s four lines from the same figures
    status reads (tests: 4 memories/34 inbox → 4 lines; empty inbox → 3 lines, no `review`; one item →
    singular). tool kit `Core 6 — Kept`, `suggestions.v1 Core 6 — Kept` after the manager put the contract''s
    literal relay sentence into both skills (the kit asserts §6''s words; the lanes'' brief had a paraphrase
    — brief defect, mine). Skill lines 18 + 21 = 39 cl100k tokens (were 150).

    2026-09-07 16:40Z — GAP: session.v3 amended in place (''ok, do it''): `list [<page>]` and `review
    [<page> | accept|decline|skip <id>…]` are markdown pages rendered by the library (bold header, `-
    **m-NNN**` bullets; numbered bold ids + blockquote quote + blank line between items), the §6 paging
    rule (≤8 one page; else ceil(n/6) pages of ceil(n/pages): 17→6·6·5, 13→5·4·4, 9→5·4), positions refused,
    multi-id = N calls, fence only for overview + receipts. Today: plain-text listing, no paging, fence
    on everything. Lane 14-A.

    2026-09-07 17:10Z — Kept, wave 14 (the manager''s gate on main @ 579f659): lane 14-A (59448d8, 678574b).
    `review` and `list` return markdown pages from the library: page_bounds() once in store.py (tests
    assert 8→1, 9→5·4, 13→5·4·4, 17→6·6·5; 45 lines→15·15·15); review page = bold header, numbered bold
    ids, quote as blockquote byte-identical to inbox.md, blank line between items, closing command line;
    `accept 2` refused naming the page''s ids; two ids = two receipts; list = `- **m-NNN**` bullets, 20/page.
    Skill: fence for overview + receipts, pages bare, `next` = page+1. tool kit `Core 6 — Kept`, `suggestions.v1
    Core 6 — Kept` against fixtures rendered from a real temp inbox; tool suite 75. Budget 422/500 after
    the `page` parameter (+12 tokens).

    2026-09-07 17:40Z — human check: the steward''s transcript b69c5e67 walked all three pages (`next`
    → page 2, 3), accepted s-010 and s-018 and declined s-015/016/017 by position ("Yes to #3", "decline
    #4, #3, and #2… #5") — the model mapped positions to ids on the current page and made one call each;
    receipts landed. Two renderings tightened at the manager''s gate (5f8bed7): a bare `>` between quote
    and source (markdown had folded them), one fence for several receipts.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-016
  title: session.v4 Core 7 — Recall is reading.
  contract:
    file: contracts/session.v4.md
    clause: Core 7
    quote: 'Recall is reading.

      '
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: 'Model behaviour (reading a topic file and saying so). Proven only in a real session.
    Still NOT-ASSERTABLE after lane C, and by design: this module is not involved in recall at all. conformance/session/tool/run.py
    reports "session.v1 Core 7 — Can''t check in this lane because reading a topic file with ordinary
    file tools and saying so is model behaviour" and prints the one structural fact that IS checkable:
    the tool offers no search operation (operations are save, forget, list), so grep over the directory
    remains the search tool, as §7 requires.

    '
  work: amplifier_bundle_memory-yvu
  notes: 'NOT exercised by lane E''s real-host smoke 2026-09-06, and said so plainly: every smoke session
    ran against a store with no topic files (caps row: topics 0/50), so no topic pointer was ever present
    to recall. This row is unchanged by the smoke and still waits on a real session with topics in the
    store.

    Lane H 2026-09-06 (notes only; disposition unchanged — NOT-ASSERTABLE). One thing did change in this
    row''s favour: a session can now WRITE a topic file. The tool''s `save` exposes the library''s `topic=`
    and `topic_purpose=`, so a ruleset goes to `topics/<slug>.md` with one pointer line left in `MEMORY.md`,
    and run.py reports `store.v1 Core 5 — Kept` with both files printed. Until now every smoke session
    ran against a store with zero topic files, so §7''s situation could not arise at all; it can now.
    The clause itself is still model behaviour and still waits on a real session with topics in the store.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
- id: AMM-017
  title: session.v4 Core 8 — Cite at use, and count it.
  contract:
    file: contracts/session.v4.md
    clause: Core 8
    quote: 'Cite at use, and count it.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/tool/run.py
  justification: '§8 promises the instrument, not the rate, and the instrument is asserted here: the `cite`
    operation writes a `cited` usage event and says nothing to the human (conformance/session/tool/run.py,
    `Core 8 (counting) — Kept`). The other half — the assistant naming a memory inline when it shapes
    an action — stays model behaviour and is reported "session.v2 Core 8 — Can''t check in this lane because
    naming a memory inline when it shapes an action (''per m-004'') is model behaviour"; the rate it feeds
    is read from real use by cli.v2 `status`.

    '
  work: amplifier_bundle_memory-yvu
  notes: 'Partly seen in a real session on this device 2026-09-06 (lane E): tests/smoke/evidence/session-C-load.txt
    cites "[m-001]" when asked what is loaded. But being ASKED to cite is not §8''s claim, which is citing
    unprompted when a memory shapes an action; the smoke never created that situation. Row unchanged,
    and the untested half is named rather than implied.

    Lane H 2026-09-06 (notes only; disposition unchanged — NOT-ASSERTABLE). The checkable remainder is
    unchanged and still true: `list` renders every line as `- [m-NNN] <text>` and `save` announces the
    new id, so an id is never unavailable at the moment of citing. Lane H adds the rule that makes a cited
    id trustworthy — `Ids are the only names. A bare number N means m-00N, never a position in a list.`
    — verbatim in the tool description and in all three skills, after the steward typed "update 4" with
    three memories on screen.

    2026-09-06 21:00Z — reseeded against v2: GAP. session.v2 §8: no `cited` event recorded; status has
    no rate. Lane K2.

    Lane K2 2026-09-06 (session.v2). §8 promises the INSTRUMENT, not the rate ("this contract promises
    the instrument, not the rate"), and the instrument now exists and is asserted: the tool has a `cite`
    operation over `amplifier_memory.record_citation`; it writes a `cited` event to usage.jsonl and returns
    nothing the human reads. conformance/session/tool/run.py reports `Core 8 (counting) — Kept` with the
    event it wrote printed. Module probes: test_cite_is_silent_and_writes_a_cited_usage_event and test_cite_of_an_unknown_id_is_the_one_line_refusal
    (`cd modules/tool-memory && uv run --offline pytest -q`, 47 passed 2026-09-06). The tool description
    and all four SKILL.md files carry, byte for byte: "When a memory changes what you would otherwise
    have done, write `per m-NNN` inline and call `cite` with that id." What this row does NOT assert:
    that the assistant actually cites unprompted when a memory shapes an action. run.py says so under
    `Core 8 (citing)`. The rate itself is cli.v2 status (lane K1) and is read from real use, not from
    this lane.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged). Ledger-integrity correction
    in the same pass: the quote this row carried, `Cite at use.`, verified against no version of the contract
    — the lead has read `Cite at use, and count it.` since v2 and the anchor was never moved with it.
    Re-anchored to the bytes; the clause text itself is unchanged v3 -> v4, so the verdict is carried,
    not re-earned.

    '
  previous_disposition: NOT-ASSERTABLE
- id: AMM-018
  title: session.v4 Core 9 — Nothing at session end.
  contract:
    file: contracts/session.v4.md
    clause: Core 9
    quote: 'Nothing at session end.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_018
  work: amplifier_bundle_memory-zqi
  notes: 'Kept 2026-09-06 for the inject hook only: the module registers exactly [''provider:request'']
    and its source contains no session:end / session_end / atexit. Probe lives in the module suite: `cd
    modules/hooks-memory-inject && uv run --offline pytest -q -k test_row_amm_018` (18 passed 2026-09-06).
    It is NOT yet discovered by the repo-root `uv run pytest` — wiring the module suites into the root
    gate is a residual for the manager. Scope caveat the manager should hold: this row is the whole bundle''s
    clause, and only one of the bundle''s modules exists so far. It must be re-checked when tool-memory
    and any Phase 2 timer land.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
- id: AMM-019
  title: session.v4 Core 10 — Fail open, never block.
  contract:
    file: contracts/session.v4.md
    clause: Core 10
    quote: 'Fail open, never block.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_019
  work: amplifier_bundle_memory-zqi
  notes: 'Kept 2026-09-06 for the inject hook only: store absent, MEMORY.md unreadable (chmod 000), and
    an unwritable error log each return action=continue with no injection and no raise; one line lands
    in ${AMPLIFIER_MEMORY_ERROR_LOG:-~/.amplifier/memory-errors.log}, deduplicated per reason per session.
    Probe lives in the module suite: `cd modules/hooks-memory-inject && uv run --offline pytest -q -k
    test_row_amm_019` (42 passed 2026-09-06). It is NOT yet discovered by the repo-root `uv run pytest`
    — wiring the module suites into the root gate is a residual for the manager. Same scope caveat as
    AMM-018: the writer and CLI have their own fail-open duty under this clause and do not exist yet.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.

    2026-09-06 21:45Z (item 87j) — the OTHER half of the clause, "the failure is one line in the transcript",
    was Broken and this row did not say so: the line was only ever set on HookResult.user_message, which
    the kernel nulls whenever any handler on the event injects, and every real session has one. Observed
    live (session f5cc2a7f): store failed, error-log line landed, terminal showed nothing. Now rendered
    through _render → DisplaySystem.show_message at level=warning, the same path lane I built for §2.
    Probes: test_row_amm_019 (asserts the rendered line), test_fail_open_line_reaches_the_display_even_with_another_injector
    (spy display + a second injecting hook on the same event — the line arrives once, our block is absent,
    one error-log line), test_fail_open_line_is_rendered_once_per_reason, test_fail_open_reason_is_one_line_and_bounded,
    test_fail_open_line_survives_the_rich_markup_channel (an unescaped bracketed reason is eaten by display.py:122
    — measured against rich 14.3.3), and the discriminating arm test_a_bad_byte_in_a_readable_store_does_not_trigger_the_fail_open_line.
    Device-checked on a real PTY: tests/smoke/evidence/failopen-turn1.txt (AMPLIFIER_MEMORY_HOME at a
    directory with no MEMORY.md; turn 1 shows the line, turn 2 shows no second one, the session answers
    normally). conformance/session/inject/run.py Core 10 now asserts the rendered line and quotes that
    capture.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
- id: AMM-020
  title: cli.v3 Core 1 — Verbs.
  contract:
    file: contracts/cli.v3.md
    clause: Core 1
    quote: 'Verbs.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_1
  work: amplifier_bundle_memory-l3e
  notes: 'Kept 2026-09-06 by lane D. probe_core_1 asserts `--help` lists exactly the eight verbs the clause
    names (init status why review doctor service update suggest), that an unknown verb prints ONE line
    and exits 2, and that `upgrade` is a hidden alias of `update` (hidden so the listing stays exactly
    eight).

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — GAP against cli.v3. Core 1 now reads `Nothing else — exactly these eight; **a
    flag is not a verb**. Every verb takes `--home <instance>`, naming the instance it acts on; without
    it the instance resolves as the store contract says (`$AMPLIFIER_MEMORY_HOME`, else the default `~/.amplifier-memory`).
    `--help` shows `--home` once, on the group, not once per verb.` Today: `grep -n -- ''--home'' src/amplifier_memory/cli.py`
    returns nothing — no verb takes it and the group does not carry it. The eight verbs themselves are
    unchanged and still Kept (init/status/why/review/doctor/service/update/suggest, cli.py:42-153). Lane
    W (l3e).

    2026-09-07 20:15Z — CONFORMS by lane W (l3e), merged 4c4b53a; the reconciler re-ran
    conformance/cli/run.py::probe_core_1 on merged main (6a64d7e): Kept — "--help lists exactly
    [''doctor'', ''init'', ''review'', ''service'', ''status'', ''suggest'', ''update'', ''why'']
    and `--home` exactly once, on the group (no verb repeats it); `--home X status`, `status --home
    X` and `--home=X status` print byte-identical output for a non-resolved instance, and a bare
    `--home` exits 2; `bogus` -> one line, exit 2; `upgrade` is a hidden alias". The eight verbs
    never moved; the half that was red — every verb takes `--home`, shown once on the group and not
    once per verb — is now measured in all three flag positions, and a flag is still not a verb.

    '
  previous_disposition: GAP
- id: AMM-021
  title: cli.v3 Core 2 — `status`
  contract:
    file: contracts/cli.v3.md
    clause: Core 2
    quote: '`status`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_2
  work: amplifier_bundle_memory-5hg
  notes: 'Kept 2026-09-06 by lane D. probe_core_2 builds a git history with backdated commits and asserts
    the exact numbers: memories=2, written 1/7d 2/30d, forgotten 1/7d, kept=1. Every number comes from
    git and usage.jsonl and nowhere else; tests/test_status.py carries the discriminating pairs for `kept`
    (same date, different presence; same presence, different date).

    2026-09-06 21:00Z — reseeded against v2: GAP. cli.v2 §2: no citation rate; kept does not treat edits
    as continuity (no edit exists yet). Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). cli.v2 §2 is complete: `status` prints ''  citation
    rate N cited / M loaded (30d)'' as a floor, and `kept` follows the pre-registered rule in docs/workflow/GATE-
    DEFINITION-2026-09-06.md — an edit keeps the original write date, and a forget + re-save of the same
    text is one lineage counted once from its first write. probe_core_2 holds both halves against a backdated
    history and both discriminate (date the edit from the edit commit, or count the re-save as new, and
    kept drops by one). tests/test_status.py carries ::test_status_prints_the_citation_rate_as_a_floor,
    ::test_kept_counts_an_edited_memory_from_its_first_write and ::test_kept_counts_a_forget_and_re_save_once_from_the_first_write.


    2026-09-07 19:10Z — carried to cli.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: CONFORMS
- id: AMM-022
  title: cli.v3 Core 3 — `why <id>`
  contract:
    file: contracts/cli.v3.md
    clause: Core 3
    quote: '`why <id>`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_3
  work: amplifier_bundle_memory-5hg
  notes: 'Kept 2026-09-06 by lane D. probe_core_3 asserts `why m-001` prints text, verbatim quote, session,
    writer and date for BOTH the save and the forget commit, and that an unknown id is one line with a
    nonzero exit.

    2026-09-06 21:00Z — reseeded against v2: GAP. cli.v2 §3: why shows no `was:`/`now:` and no `forgot`
    marker. Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). cli.v2 §3 is complete: `why <id>` prints the creation,
    each edit as `was: "<old>" → now: <new>`, and a forget headed `forgot`. probe_core_3 asserts the three
    block headings are exactly [''save'', ''edit'', ''forgot''] (oldest first) and that the edit line
    carries both texts; an unknown id is still one line, exit 1. tests/test_cli.py::test_why_shows_the_creation_each_edit_as_was_now_and_a_forget_marked_forgot.


    2026-09-07 19:10Z — carried to cli.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: CONFORMS
- id: AMM-023
  title: cli.v3 Core 4 — `review`
  contract:
    file: contracts/cli.v3.md
    clause: Core 4
    quote: '`review`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_4
  work: amplifier_bundle_memory-5hg
  notes: 'Kept 2026-09-06 by lane D. probe_core_4 asserts an empty inbox says so and exits 0. The non-empty
    path is exercised by tests/test_status.py; the suggestions themselves are Phase 2 (suggestions.v1,
    still DRAFT).

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to cli.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-024
  title: cli.v3 Core 5 — `doctor`
  contract:
    file: contracts/cli.v3.md
    clause: Core 5
    quote: '`doctor`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_5
  work: amplifier_bundle_memory-l3e
  notes: 'Kept 2026-09-06 by lane D. probe_core_5 hashes all 40 files in the store (including .git/) before
    and after a full doctor run and asserts byte-identity; asserts the row set is exactly store/caps/stale
    topics/inbox/suggest timer/substrate/update; asserts the update trio WARN(behind, remedy named)/OK(current)/INFO(not
    checkable) with the shas injected so no test touches the network; and asserts exit 1 only when the
    store check FAILs (a WARN update does not). The suggest-timer and substrate rows are INFO because
    Phase 2 is not installed, which is what the clause itself prescribes.

    2026-09-06 21:00Z — reseeded against v2: GAP. cli.v2 §5: the well-formed check lives inside the `store`
    row, not as its own `MEMORY.md well-formed` row; --repair exists. Lane K1.

    2026-09-06 21:30Z — CONFORMS again (lane K1). cli.v2 §5''s row list is built: `doctor` now prints
    store · caps · MEMORY.md well-formed · stale topics · inbox · suggest timer · substrate · update,
    with well- formedness split out of the `store` row. probe_core_5 asserts the new row list, byte-identity
    before/after, and that the well-formed row FAILs (exit 1) on a headless fragment (naming line 2) and
    on a non-UTF-8 byte (naming the offset), each naming the last clean commit and `doctor --repair`,
    while the `store` row stays OK — the discriminating half, since one FAIL used to answer both questions.
    `doctor --repair` is unchanged. tests/test_cli.py::test_doctor_prints_its_own_wellformed_row and tests/test_doctor.py::test_doctor_names_a_malformed_memory_file_the_line_and_the_remedy.

    2026-09-06 22:10Z — measured on the steward''s device: GAP. cli.v2 §5 update check: compares only
    the uv tool''s commit against main; the bundle cache (what modules/skills load from) and the amplifier-venv
    library (what modules import) were both behind while doctor said ''current''. Lane M (bbh).

    2026-09-06 23:10Z — CONFORMS again (lane M). cli.v2 §5''s update check reads all THREE installed things:
    the uv tool, the bundle cache clone(s), and the `amplifier_memory` inside the amplifier CLI''s own
    venv (`installed_commits()` in doctor.py). probe_core_5 asserts OK names all three equal to main,
    and — the discriminating half — that with any ONE leg stale the row is WARN naming THAT leg (`bundle
    cache <old> behind main <new>`) plus the remedy, which is exactly the silence measured at 22:05Z (a
    current uv tool reading `[OK] update current` over a four-waves-old cache). A leg that cannot be found
    is INFO, never RED. tests/test_doctor.py::test_update_check_names_which_of_the_three_is_behind (parametrized
    over all three legs) and ::test_a_leg_that_cannot_be_found_is_info_never_red; doctor is still byte-identical
    before/after.


    2026-09-07 19:10Z — GAP against cli.v3. §5 gains a row: `**judge model** — the provider and model
    the daily pass will use, or, while the host cannot resolve a role for `amplifier run`, `inherits the
    app''s default` naming that default and the last run''s measured cost, so an inherited price is visible
    rather than silent (Phase 2)`. Today llm_row (doctor.py:490-530) prints `inherits the CLI default
    (no memory-config.toml at <path>)` — it names neither the app''s default itself nor any measured cost,
    and its tail carries only `last run used provider=<p>`. It also reads `~/.amplifier/memory-config.toml`,
    which store.v3 §2 retires into the instance''s config.yaml. Every other §5 row is unchanged and still
    Kept. Lane W (l3e).

    2026-09-07 20:15Z — CONFORMS by lane W (l3e), merged 4c4b53a; the reconciler re-ran
    conformance/cli/run.py::probe_core_5 on merged main (6a64d7e): Kept — "46 files byte-identical
    before/after; rows [''store'', ''caps'', ''MEMORY.md well-formed'', ''stale topics'', ''inbox'',
    ''suggest timer'', ''substrate'', ''llm judge'', ''update'']; […] exit 1 only on a failed
    check". NAMED LIMIT, not assumed away: probe_core_5 asserts the judge row EXISTS in §5''s row
    set — it never reads its wording, which is the half this row was red for. Two other observations
    in this same pass are what earn the flip, and they are named here rather than implied. (1) The
    row rendered live on a fresh instance (PYTEST_CURRENT_TEST set, temp home): `inherits the app''s
    default (whatever `amplifier run` selects with no -p/-m/-B) · role fast (recorded; this host
    cannot resolve a role for `amplifier run`) · config.yaml · no run yet, so no measured cost` —
    the default named, the file named, the measured cost said as a real answer when there is no run.
    (2) `uv run pytest -q tests/test_doctor.py -k llm_row` → 4 passed, asserting INHERITED,
    APP_DEFAULT, config.yaml, `role fast` and the cost sentence are all in the detail, and that a
    configured judge does not inherit. RESIDUAL for the next cli-kit lane: move those assertions
    into probe_core_5, so the kit alone carries the clause; and doctor''s llm_row and
    suggest.judge_detail still render the same fact two ways (the lanes'' own carried residual,
    CHECK-RECORD 15b).

    '
  previous_disposition: GAP
- id: AMM-025
  title: cli.v3 Core 6 — `service`
  contract:
    file: contracts/cli.v3.md
    clause: Core 6
    quote: '`service`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_6
  work: amplifier_bundle_memory-l3e
  notes: 'CONFORMS 2026-09-06 by lane P: the clause''''s substance is built. probe_core_6 now installs
    against a temp unit dir with a fake runner and asserts the units render (Type=oneshot with no [Install];
    OnCalendar=daily, Persistent=true; ExecStart=<abs amplifier-memory> suggest), that install runs exactly
    `systemctl --user daemon-reload` then `enable --now`, that `service status` through the CLI reads
    it back, that uninstall leaves nothing, and that a failing enable step rolls back every file the call
    wrote. It discriminates -- the rollback arm fails if either unit survives. The Phase 1 sentence this
    row previously read Not yet for is gone: `service` now manages a real timer. Also tests/test_service.py
    (14 tests). The verbs start/stop/restart/logs run nothing at all when no timer is installed, which
    is what `update`''''s step 4 asks on every run.


    2026-09-07 19:10Z — GAP against cli.v3. §6 is now per instance: `**per instance**: `install`, `uninstall`,
    `status`, `restart` and the rest act on the instance `--home` resolves to, and each unit name is derived
    from that instance''s path — so two instances never collide and neither can silently uninstall the
    other''s timer.` and ``status` lists every installed instance timer, not only the resolved one.` Today
    UNIT_BASE is the single fixed string ''amplifier-memory-suggest'' (service.py:61), so one device has
    exactly one timer and a second instance would overwrite the first''s units; `status` reports that
    one unit only. The render / daemon-reload -> enable --now / rollback half is unchanged and still Kept.
    Lane W (l3e).

    2026-09-07 20:15Z — CONFORMS by lane W (l3e), merged 4c4b53a; the reconciler re-ran
    conformance/cli/run.py::probe_core_6 on merged main (6a64d7e): Kept — "install writes
    [''amplifier-memory-suggest-memory-abde2f43.service'',
    ''amplifier-memory-suggest-memory-abde2f43.timer''] into the unit dir - the unit name carries
    the instance (memory-abde2f43) and ExecStart is `<abs> suggest --home <instance>` […] a second
    instance adds its own two units […] `installed_timers` lists both instances
    ([''/tmp/cli-v2-conformance-h1ccb39u/memory'',
    ''/tmp/cli-v2-conformance-h1ccb39u/second-instance'']) marking one resolved, and uninstalling
    the second leaves the first […] `service status --home` through the CLI reads it back and exits
    0; uninstall leaves nothing; a failing enable rolls back every file written (left []) - all
    against a temp unit dir with a fake runner, never this device". The red half is measured in both
    directions: two instances get two unit pairs and neither uninstall touches the other, and
    `status` lists every installed instance timer, not only the resolved one. The render /
    daemon-reload -> enable --now / rollback half is unchanged and still Kept.

    '
  previous_disposition: GAP
- id: AMM-026
  title: cli.v3 Core 7 — `update`
  contract:
    file: contracts/cli.v3.md
    clause: Core 7
    quote: '`update`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_7
  work: amplifier_bundle_memory-16w
  notes: 'Kept 2026-09-06 by lane E. probe_core_7 exercises amplifier_memory.run_update with a recording
    runner and asserts the four steps in order: uv tool upgrade amplifier-memory; amplifier bundle remove
    then add the behavior URI with --app; the timer skipped because service_status says Phase 1 has none;
    doctor. It also asserts the stale-in-memory note and that the store''s git history is unchanged. Run
    for real on this device the same day: every step ok, doctor exit 0. Each argv is verified against
    that CLI''s own --help by tests/test_update.py, which prints the help (AGENTS.md rule 5). Step 2 is
    a remove-then-add because `amplifier bundle update` cannot reach an app bundle registered by URI (measured
    three ways: by name -> "No handler for URI"; with --source -> "No active bundle."; --all enumerates
    registry bundles only), and the URI carries #subdirectory=behaviors/memory-session.yaml because the
    root-bundle URI is a self-include the loader skips.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.

    2026-09-06 22:10Z — measured on the steward''s device: GAP. cli.v2 §7 update: refreshes the uv tool
    only; `bundle remove`+`add` did not refetch the cache (stayed at 0afc6a8) and the library inside the
    amplifier venv was never refreshed — the steward''s device ran v1 words after waves 5–8 were ''installed''.
    Repaired by hand 22:00Z; lane M (bbh) makes update do it.

    2026-09-06 23:10Z — CONFORMS again (lane M). `update` now refreshes all three: uv tool upgrade; `git
    -C <clone> fetch origin` + `reset --hard origin/main` for every cache clone (cache/ and cache/skills/);
    `uv pip install --python <amplifier venv python> --refresh --reinstall-package amplifier-memory "amplifier-memory
    @ git+<repo>@main"` — the two commands that repaired the device by hand at 22:00Z, encoded rather
    than redesigned. Each is its own StepResult line carrying `<old7> → <new7>`. probe_core_7 runs it
    against a FAKE DEVICE (temp cache clones off a local-disk origin, temp venv carrying a PEP 610 direct_url.json)
    and asserts the clones MOVED by reading `git rev-parse` off disk afterwards, not the step''s own claim;
    also that the no-venv branch is one warn line with the other steps still running, and that `bundle
    add` remains the install path when there is no clone. Each argv verified against its own --help by
    tests/test_update.py (AGENTS.md rule 5), which prints it.

    2026-09-07 — lane N: one run is enough. The process running `update` IS the pre-upgrade CLI, so the
    first run after an upgrade used to finish with the old code (measured 22:25Z: old steps, old single-leg
    doctor row; only the second run refreshed the cache and the venv). `run_update` now reads installed_commit()
    either side of step 1 and, if it moved, hands the rest of the run to the freshly installed binary
    — os.execv(<amplifier-memory>, [..., "update", "--after-upgrade"]), the hidden flag skipping step
    1 so it is a hand-off and not a loop. probe_core_7 exercises all three sides against a fake device
    with a scripted commit reader and a stood-in os.execv: the hand-off (execv argv recorded; the old
    process ran step 1 and nothing else, its clones still on the old commit), the re-executed process
    (step 1 skipped as "already upgraded by the previous process", both clones and the venv library really
    moved, doctor ran, exit 0), and a hand-off that cannot happen (nothing on PATH → ONE [info] line "upgraded
    in place — the steps below ran with the previous version; run update once more", every other step
    still run, exit 0). NOT checked here: that a real `uv tool upgrade` is visible to installed_commit()
    in the running process — a device fact; the manager''s real run after merge is what proves it, and
    if it is not, the fallback is exactly the previous behaviour, which doctor already catches with WARN.


    2026-09-07 19:10Z — carried to cli.v3 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-027
  title: cli.v3 Core 8 — `init`
  contract:
    file: contracts/cli.v3.md
    clause: Core 8
    quote: '`init`

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_8
  work: amplifier_bundle_memory-l3e, amplifier_bundle_memory-azy
  notes: 'Kept 2026-09-06 by lane D. probe_core_8 asserts the first `init` creates exactly the store.v1
    Core 2 layout in one commit and the second reports the store exists and leaves `git log --oneline`
    unchanged. The library init() was proven in wave 1; this row is the VERB.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 16:40Z — GAP: cli.v2 §8 amended in place (''ok, do it''): `init` installs the daily timer
    as `service install` does, prints the opt-out (`service uninstall`) and the config path, takes `--no-timer`;
    second run says ''store exists · timer installed''; Phase 1 host prints §6''s no-service line. Today:
    `init` creates the store only. Lane 14-B.

    2026-09-07 17:10Z — Kept, wave 14 (the manager''s gate on main @ 579f659): lane 14-B (22758fe) + the
    §8 amendment of 17:00Z. `init` creates the store then calls service.install — the one install function
    — for the device store ~/.amplifier/memory only (any other path: store + one-line pointer to `service
    install`, the ratified CANDIDATE-init-device-store); prints the opt-out and the config path; second
    run `store exists · timer installed`, writes nothing; `--no-timer` leaves no unit; Phase 1 arm prints
    install''s own refusal. cli kit `Core 8 — Kept` with all four arms via the fake runner + temp UNIT_DIR;
    tests/test_init_timer.py 30 passed; the real units on this device untouched (mtime 21:07 09-06). Device
    check of the real path (`init` against ~/.amplifier/memory) is Can''t check without a fresh device
    — the store already exists here.


    2026-09-07 19:10Z — GAP against cli.v3. §8 is rewritten: `builds the instance at the resolved home
    (`--home`, else `$AMPLIFIER_MEMORY_HOME`, else the default `~/.amplifier-memory`)`; `it offers to
    move the older store to the default and prints what it did — never a silent move`; `It writes `config.yaml`
    inside the instance with the shipped defaults: `enabled: true`, and `llm: judge: {role: fast, provider:
    "", model: "", bundle: ""}``; `It asks **one** question, `What kinds of things should I remember for
    you?`, ... and saves what the human types or accepts as `m-001`, writer=human, that text also being
    the quote`; and `the unit name carries the instance, so `init` works for any instance rather than
    only the device store`. Today init() (store.py:789-810) creates the layout and commits, writes no
    config.yaml, asks nothing, offers no move, and installs the one device-wide unit for `~/.amplifier/memory`
    only — the v2 device-store rule this clause replaces. The related live defect amplifier_bundle_memory-azy
    (a redirected store still reports the timer installed outside pytest) sits in the same clause. Not
    a regression: this row was Kept against cli.v2 §8 at wave 14 (0156865) and nothing in the code moved
    — the clause did. Lane W (l3e).

    2026-09-07 20:15Z — CONFORMS by lane W (l3e) and the azy fix, merged 4c4b53a; the reconciler
    re-ran conformance/cli/run.py::probe_core_8 on merged main (6a64d7e): Kept — "first init created
    [''MEMORY.md'', ''config.yaml'', ''declined.md'', ''inbox.md'', ''sessions.jsonl'', ''topics'',
    ''usage.jsonl''] in two commits […] m-001 carries the seeding answer as writer=human with the
    answer as its own quote; the second changed nothing and said so in one line. The install-plane
    guard is armed (''a test is running (PYTEST_CURRENT_TEST is set)''), so a standalone kit run
    cannot reach real systemctl (azy). Timer arms, all against a temp unit dir with a fake runner,
    installs -> units [''amplifier-memory-suggest-memory-1539bcf1.service'',
    ''amplifier-memory-suggest-memory-1539bcf1.timer''] […] Move offer -> declined -> ''left […]
    where it is (not moved); this instance is […]. Move it yourself any time: mv […]''; accepted ->
    ''moved […] to […] — the same store, at the default''" (excerpt of the run this note records).
    All four v3 demands are measured: the move offer in both answers and never silently, config.yaml
    with the shipped defaults, the one seeding question saved as m-001 writer=human with the answer
    as its own quote, and a unit name carrying the instance so init works for any instance. NOT
    re-run here and honestly named: the STANDALONE arm (no PYTEST_CURRENT_TEST, the guard down) that
    proves azy against this device''s real timer — that is the manager''s gate evidence in
    CHECK-RECORD 15b (ActiveEnterTimestamp byte-identical before/after), not this pass''s; this pass
    ran the kit with the guard armed so nothing could touch the device.

    '
  previous_disposition: GAP
- id: AMM-028
  title: cli.v3 Core 9 — Thin wrapper.
  contract:
    file: contracts/cli.v3.md
    clause: Core 9
    quote: 'Thin wrapper.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/cli/run.py::probe_core_9
  work: amplifier_bundle_memory-5hg
  notes: 'Kept 2026-09-06 by lane D. probe_core_9 asserts cli.py''s import lines are exactly [''import
    click'', ''import amplifier_memory''], and that every verb''s behaviour (init/status/review/doctor/service_status/suggest_status/update_plan/update_check)
    runs in a subprocess with click absent from sys.modules. Behaviour lives in src/amplifier_memory/{store,status,doctor}.py;
    cli.py parses, calls once, prints, exits.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to cli.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-029
  title: store.v3 Core 3 — MEMORY.md is a flat list of memories, one per line, capped at 200 lines
  contract:
    file: contracts/store.v3.md
    clause: Core 3
    quote: 'MEMORY.md is a flat list of memories, one per line, capped at 200

      lines

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_3
  notes: 'Added 2026-09-06 by the manager session after wave 1: the seeding pass missed this clause because
    its bold lead spans a line break. Not renumbered — ids are stable. probe_core_3 printed Kept in the
    manager''s own run (200 lines incl. a heading and a blank line; line form `- [m-NNN] text`; 198 memories
    parsed).

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to store.v3 2026-09-07 (clause unchanged).

    '
- id: AMM-030
  title: session.v4 R2 — sub-agent sessions never save
  contract:
    file: contracts/session.v4.md
    clause: R2
    quote: 'Sub-agents inherit the injected block (they

      work for the same human) but **never save**: only a root session with a

      human interlocutor may write.

      '
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/tool/run.py::check_r2
  notes: 'Added 2026-09-06 by the manager session after wave 2: R2 sits under Reserved, not Core, so the
    seeding pass (Core clauses only) left it without a row although it has teeth. check_r2 printed Kept
    in the manager''s own run — save and forget refuse before any library call when coordinator.parent_id
    is set. The "inherit the injected block" half is the hook''s provider:request registration (AMM-010),
    which does not consult parent_id.

    2026-09-06 21:00Z — re-read against v2: clause text unchanged in substance; verdict carried.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
- id: AMM-037
  title: suggestions.v2 Core 1 — A timer, not a service
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 1
    quote: A timer, not a service.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_1
  work: amplifier_bundle_memory-b0g
  notes: 'Kept 2026-09-06 by lane P. probe_core_1 asserts the rendered units: the service is Type=oneshot
    with no [Install] (only the timer starts it) and ExecStart=<abs amplifier-memory> suggest; the timer
    is OnCalendar=daily, Persistent=true. install runs `systemctl --user daemon-reload` then `enable --now`,
    uninstall leaves nothing, and a failing enable step removes every unit file the call wrote -- all
    against a temp unit dir with a fake runner. NOT checked here: that a real systemd leaves nothing resident
    after a run; installing one is exactly what a kit must not do (this lane enabled a real timer on the
    steward''s device that way on 2026-09-06 and removed it), so the manager''s run after merge is what
    proves it. Also tests/test_service.py and conformance/cli/run.py::probe_core_6.


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-038
  title: 'suggestions.v2 Core 2 — Input: recorded sessions'
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 2
    quote: 'Input: recorded sessions.'
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_2
  work: amplifier_bundle_memory-h3r
  notes: 'Kept 2026-09-06 by lane P. probe_core_2 builds five recorded sessions and asserts exactly one
    is read: the sub-agent id (0000000000000000-<hex>_<agent>) is not a root UUID, a one-turn session
    is below the two-turn floor, a three-day-old session is outside the 24h window, and a session whose
    first human turn is this job''s own prompt is excluded. max_sessions takes the newest first. The layout
    it reads is the one measured on this device 2026-09-06: <base>/<project>/sessions/<id>/{metadata.json,transcript.jsonl},
    with metadata.timestamp on each turn.


    2026-09-07 19:10Z — GAP against suggestions.v2. Core 2 makes the human interlocutor checkable: `a
    session is read only when **both** hold. (a) **Its recorded origin is `human`** — from the instance''s
    `sessions.jsonl` (store §2), written at start from `AMPLIFIER_SESSION_ORIGIN` (session §13); **no
    record counts as `human`**` and `(b) **It has ≥2 human turns of typed text in the last 24 hours**
    — and two measured shapes are not typed text: a **lane brief** ... and a **system-reminder-only continuation**.`
    Today select_sessions (suggest.py:263-303) reads root-vs-sub-agent off the session-id SHAPE, counts
    every human turn as typed text, and knows nothing of origins — sessions.jsonl is written by nobody
    (grep: 0 hits in src/ and modules/). Half (a) is unreachable until session §13 lands (lane V, 8o4).
    The ≤30 / newest-first / 24h window half is unchanged and still Kept. Lane X (h3r).

    2026-09-07 20:15Z — CONFORMS by lane X (h3r), merged 95234f5, on the session-plane input lane V
    landed (b9e9eef); the reconciler re-ran conformance/suggestions/run.py::probe_core_2 on merged
    main (6a64d7e): Kept — "of seven recorded sessions only bc214bdf is read. Refused: 6bafabaf,
    whose sessions.jsonl origin is `worker` (counted, origin_excluded=1 in the run''s line);
    ff77aa88, whose turns are a lane brief (opens ''Claim <id> from the <project> work-tracker
    project'', >1500 chars) and a system-reminder-only continuation, so it has zero typed-text
    turns; the sub-agent id (0000000000000000-<hex>_<agent>); one human turn (below the two-turn
    floor); a three-day-old session (outside 24h); and a session whose first human turn is this
    job''s own prompt. It discriminates in both directions: with NO origins map the worker session
    IS read, because `no record counts as human`, while the briefed one is still refused. The run
    made exactly 1 model call, and neither the brief nor the reminders appear in the request the
    judge saw; with max_sessions=3 the newest three are taken, in order". Both halves of the v2
    change are measured: (a) the recorded origin is read from the instance''s sessions.jsonl, with
    `no record counts as human` proven by the discriminating arm, and (b) the two measured non-typed
    shapes are excluded. The ≤30 / newest-first / 24h window half is unchanged and still Kept.

    '
  previous_disposition: GAP
- id: AMM-039
  title: suggestions.v2 Core 3 — One question, one call per session
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 3
    quote: One question, one call per session.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_3
  work: amplifier_bundle_memory-h3r
  notes: 'Kept 2026-09-06 by lane P. probe_core_3 lifts the sentence out of contracts/suggestions.v1.md''s
    own quotation marks and asserts suggest.PROMPT is byte-identical to it -- retyping it here would let
    the clause and the code drift silently. The prompt actually sent carries <MEMORY.md> and <declined.md>
    filled with the store''s real lines, and exactly one call is made per session. The default model call
    is `amplifier run --output-format json` (NOT --output: verified against that CLI''s own --help, printed
    by tests/test_suggest.py, AGENTS.md rule 5) and is injectable, so no check ever calls a model.

    2026-09-07 wave 12 (lane R): compose_request now fences the human turns as quoted data, not instructions;
    the §3 sentence is still the first line character for character (test_the_prompt_is_section_3_verbatim).
    Measured basis: a /goal transcript hijacked haiku 3 times in 70 calls.


    2026-09-07 19:10Z — GAP against suggestions.v2. Core 3 now also names WHICH model answers: `**Which
    model answers it** is resolved from the instance''s `config.yaml` `llm: judge:`, in this order: `provider`/`model`/`bundle`
    when set; else the **role** — `fast` as shipped — resolved through the host''s routing when it can
    (`amplifier run --model-role`); else the app''s own default, inherited.` Today the resolution reads
    `${AMPLIFIER_MEMORY_CONFIG:-~/.amplifier/memory-config.toml}` `[llm.judge]` (llm_config.py:14-57),
    not the instance''s config.yaml — store.v3 §2 retires that file. DEFAULT_ROLE is already `fast` and
    is already never a provider id (llm_config.py:63-78), so that half is earned; the file it is read
    from is not. The QUESTION half is untouched by v2 and still Kept: suggest.PROMPT is lifted byte-for-byte
    from the contract''s own quotation marks (test_the_prompt_is_section_3_verbatim). Lane X (h3r).

    2026-09-07 20:15Z — CONFORMS by lane X (h3r), merged 95234f5; the reconciler re-ran
    conformance/suggestions/run.py::probe_core_3 on merged main (6a64d7e): Kept — "suggest.PROMPT is
    byte-identical to the sentence inside §3''s own quotation marks (lifted from
    contracts/suggestions.v2.md, not retyped); the prompt actually sent carries <MEMORY.md> and
    <declined.md> filled with the store''s real lines, and exactly one call was made for one
    session. Which model answers it follows §3''s order over the INSTANCE''s config.yaml (store.v3
    §2): provider/model set -> `-p luna -m gpt-5.6-luna`; unset on a host whose `amplifier run
    --help` documents --model-role -> `--model-role fast`, the shipped ROLE; unset on a host without
    it -> inherited, adding no flag, so the argv is byte for byte what it always was. The shipped
    default carries no provider id, because a provider id names one machine''s account". The file
    the resolution reads is now the instance''s config.yaml, which is the change this row was red
    for, and the QUESTION half is untouched. NAMED LIMIT, measured on this host rather than assumed:
    resort 2 — the role resolved through the host''s routing — cannot fire here, because `amplifier
    run --help` documents no `--model-role` (amplifier 2026.09.07-ea87c0c, core 1.6.1; grep found
    nothing). The clause itself says "when it can", so this is not a divergence; the probe exercises
    the role branch against a stand-in `--help` text, which is a check of the code''s decision
    boundary, not an observation of a host that can route roles. What this host really does — resort
    1 when config.yaml names a provider, resort 3 inherited and NAMED otherwise — is what the probe
    checks against the real resolution, and is what earns CONFORMS. When `amplifier run` grows a
    role flag upstream, this row is re-reviewed against a real host.

    '
  previous_disposition: GAP
- id: AMM-040
  title: suggestions.v2 Core 4 — Code verifies before it proposes
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 4
    quote: Code verifies before it proposes.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_4
  work: amplifier_bundle_memory-b0g
  notes: 'Kept 2026-09-06 by lane P. probe_core_4 is the discriminating pair plus the poisoning arm: a
    candidate whose quote appears in no human turn is rejected and counted and never reaches inbox.md;
    the ones whose quotes are verbatim land in §4''s shape with quote and session id; a text already in
    MEMORY.md is not proposed again over the same transcript. Recorded honestly in the probe''s own evidence:
    a task instruction''s quote IS verbatim, so §4''s code check passes it -- what keeps task instructions
    out is §3''s question, which is the model''s half, not code''s.

    2026-09-07 wave 12 (lane S, acu, merged caee5f5): the already-known set in inbox.append is keyed on
    the verbatim quote as well as the text (pending items and MEMORY.md); declined.md carries text+date
    only, so a declined suggestion returning paraphrased still slips - pinned by test_declined_dedupe_is_text_only_today
    and one README sentence. Measured basis: prompt-level dedupe missed 0-30% across 7 models.


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-031
  title: suggestions.v2 Core 5 — Surface without interrupting
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 5
    quote: Surface without interrupting.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/inject/run.py::check_suggestions_5
  work: amplifier_bundle_memory-862
  notes: 'Seeded 2026-09-06 23:05Z by the manager on ratification: Not yet — nothing of the suggestion
    inbox existed in the repository.

    2026-09-06 23:20Z, lane Q — the SURFACE is built and asserted; the row stays GAP because the half
    it names cannot be reached from this lane. check_suggestions_5 reports "Can''t check", and says why
    in the words the reader gets: amplifier_memory.inbox is not in this build (lane P owns it), so the
    count comes from a stand-in at lane P''s published signature `pending(home) -> list[Suggestion]`,
    not from a real inbox.md. Proven against that stand-in, printed by the kit and by modules/hooks-memory-inject/tests/test_inject.py:
    3 waiting renders the load line then exactly "3 suggestions waiting. /memory review to see them.",
    1 renders the singular "1 suggestion waiting. /memory review to see it.", 0 renders nothing, and request
    2 renders nothing at all; the line returns once after a compaction and never between; the injected
    block is byte-identical with and without an inbox (sha256 dd02a7cc0ffa24e6…) and carries no suggestion
    id, text or count — §5''s "only accepted memories are loaded" holds by construction, not by care;
    no inbox in the build is a surface that is off, not an error; an inbox that raises costs one error-log
    line, no line on screen, and the memories are still loaded. Flip to CONFORMS when lane P''s library
    lands and the kit reports Kept without a stand-in. Flipped to CONFORMS 2026-09-06 23:55Z by the manager
    in the wave-11 post-merge gate: with lane P''s real amplifier_memory.inbox on main, check_suggestions_5
    reports Kept against the real library (CHECK-RECORD 11).


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
- id: AMM-032
  title: suggestions.v2 Core 6 — Review is one keystroke per item
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 6
    quote: Review is one keystroke per item.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/tool/run.py::check_suggestions_6
  work: amplifier_bundle_memory-862
  notes: 'Seeded 2026-09-06 23:05Z by the manager on ratification: Not yet — nothing of the suggestion
    inbox existed in the repository.

    2026-09-06 23:20Z, lane Q — the SURFACE is built and asserted; the row stays GAP for the same reason
    as AMM-031. check_suggestions_6 reports "Can''t check": accept''s write, decline''s declined.md line
    and skip''s leave-it-waiting belong to amplifier_memory.inbox, which is not in this build, so they
    are a stand-in at lane P''s published signatures (`accept(sid, home, *, session_id)`, `decline(sid,
    home)`, `skip(sid, home)`). Proven against that stand-in, byte-compared to modules/tool-memory/tests/fixtures/review-lines.txt
    by both the kit and the module suite: the listing for three, for one and for none; accept → session.v2
    §3''s three lines with the third reading "  suggested from session bc214bdf, accepted by you", reaching
    the library with this session''s id; decline → "declined s-042 — won''t be proposed again. Reverse
    by hand: edit declined.md"; skip → "skipped s-042 — still waiting."; an unknown id → "no suggestion
    s-999. Waiting: s-042, s-043, s-044."; a build with no inbox → "review is not available in this build;
    run amplifier-memory update"; session.v2 R2 — a sub-agent may list the inbox and may not accept or
    decline. skills/memory/SKILL.md documents /memory review with all three actions and the no-restate
    rule; bundle.md carries its row. The end-to-end arm exists and is honestly skipped: test_accept_writes_through_the_real_library
    carries skipif(not hasattr(amplifier_memory, "inbox")). Flip to CONFORMS when lane P lands and that
    test runs. Flipped to CONFORMS 2026-09-06 23:55Z by the manager in the wave-11 post-merge gate: with
    lane P''s real amplifier_memory.inbox on main, check_suggestions_6 reports Kept against the real library
    (CHECK-RECORD 11).


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
- id: AMM-033
  title: suggestions.v2 Core 7 — Never re-propose a decline
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 7
    quote: Never re-propose a decline.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_7
  work: amplifier_bundle_memory-b0g
  notes: 'Kept 2026-09-06 by lane P. probe_core_7 declines a proposal and runs again over the same transcript:
    it is not re-proposed. The match is exact but whitespace-normalised, so an editor''s stray space cannot
    resurrect a decline, and the same list is handed to the prompt as §3 asks. It discriminates -- remove
    the declined.md check and the item returns to the inbox.


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-034
  title: suggestions.v2 Core 8 — Bounded cost, visible
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 8
    quote: Bounded cost, visible.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_8
  work: amplifier_bundle_memory-h3r, amplifier_bundle_memory-l3e
  notes: 'Kept 2026-09-06 by lane P. probe_core_8 sets max_calls=2 over four eligible sessions: exactly
    two calls are made, the rest are skipped and named in the run''s status rather than queued. doctor''s
    `suggest timer` row reads installed/enabled/last run/last outcome and `inbox` reads size + oldest,
    and no Phase 2 row can fail the check (cli.v2 Core 5: nonzero only on a failed check).

    2026-09-07 wave 12 (lane R, ec7, merged bf01606): cost is now also STEERABLE - `${AMPLIFIER_MEMORY_CONFIG:-~/.amplifier/memory-config.toml}`
    `[llm.judge] provider/model/bundle/role` -> `amplifier run -p/-m/-B`; the suggest.log line names `provider=`;
    doctor shows the resolved choice. Measured basis: evaluations/model-class/RESULTS-2026-09-06-pilot.md
    (luna $0.02/call vs the inherited opus $0.28).


    2026-09-07 19:10Z — GAP against suggestions.v2. Core 8 now demands the judge be NAMED: `**and names
    the judge**: the configured provider/model, or the role it resolved through, or `inherited` with the
    app''s default and its measured per-call cost, so an unattended night''s bill is never learned afterwards.`
    Today doctor''s `llm judge` row prints the configured provider/model or `inherits the CLI default`,
    plus the recorded role and the last run''s provider (doctor.py:490-530) — it never names the app''s
    default nor any measured per-call cost, the one number the clause was written for (evaluations/model-class/RESULTS-2026-09-06-pilot.md:
    $0.276/call inherited vs $0.02 chosen). The ≤30-calls/one-run-a-day bound and its skip-and-report
    behaviour are unchanged and still Kept. Lanes X (h3r) and W (l3e) — the row lives in doctor.py, the
    number comes from the job.

    2026-09-07 20:15Z — CONFORMS by lanes X (h3r) and W (l3e), merged 95234f5 and 4c4b53a; the
    reconciler re-ran conformance/suggestions/run.py::probe_core_8 on merged main (6a64d7e): Kept —
    "the ceiling is 30 calls per run; with max_calls=2 over 4 eligible sessions the run made exactly
    2 calls, skipped the other 2 and said so in its status […] The judge is NAMED in all three
    states by one library function, `suggest.judge_detail`, which doctor''s `llm judge` row is a
    thin adapter over: a configured provider (''provider luna (config.yaml)''); the role this host
    resolved (''role fast, resolved by this host (`amplifier run --model-role fast`)
    (config.yaml)''); or inherited, with the app''s default AND its measured per-call cost -
    $0.276/call from evaluations/model-class/RESULTS-2026-09-06-pilot.md, 2026-09-06, up to $8.28
    for a full night - so an unattended night''s bill is read before the night, never after it".
    That last state is exactly what the row was red for: the app''s default named, and the measured
    per-call cost beside it, before the night rather than after it. The ≤30-calls / one-run-a-day
    bound and its skip-and-report behaviour are unchanged and still Kept. Residual carried from the
    lanes, not re-litigated here: doctor''s llm_row renders the same fact in its own words rather
    than calling judge_detail (cosmetic unification later, CHECK-RECORD 15b); the role state above
    is rendered against a stand-in `--help`, since this host resolves no role (see AMM-039).

    '
  previous_disposition: GAP
- id: AMM-035
  title: suggestions.v2 Core 9 — Report, even when empty
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 9
    quote: Report, even when empty.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_9
  work: amplifier_bundle_memory-h3r
  notes: 'Kept 2026-09-06 by lane P. probe_core_9 runs twice and asserts exactly two lines in suggest.log,
    each carrying sessions/proposed/rejected/dropped_stale/calls/status; a run that proposed nothing still
    reported, with status=ok. The log leaves the store''s tree clean: it is a run log, not memory (store.v2
    §2 fixes the layout), so it is excluded through .git/info/exclude -- where the write lock already
    lives -- rather than committed or left dirty. status is parsed as everything after `status=`, because
    a degraded reason is a sentence and splitting it on space reported `degraded:substrate` for `degraded:substrate
    missing`.


    2026-09-07 19:10Z — GAP against suggestions.v2. Core 9''s log line gains a field: `sessions read,
    **sessions refused by origin (`origin_excluded=N`, beside `sessions=`)**, proposed, rejected, dropped-as-stale.`
    Today the line is `sessions=<n> proposed=<n> rejected=<n> dropped_stale=<n> calls=<n> provider=<p>
    status=<s>` (suggest.py:145-156) — no origin_excluded field, because nothing counts an origin refusal
    yet (Core 2, same lane). One-line-per-run, always, even at zero proposed, is unchanged and still Kept.
    Lane X (h3r).

    2026-09-07 20:15Z — CONFORMS by lane X (h3r), merged 95234f5; the reconciler re-ran
    conformance/suggestions/run.py::probe_core_9 on merged main (6a64d7e): Kept — "two runs left
    exactly two lines in suggest.log - ''2026-09-07T20:14:10+00:00 sessions=1 origin_excluded=1
    proposed=0 rejected=0 dropped_stale=0 calls=1 provider=inherited status=ok'' and
    ''2026-09-07T20:14:10+00:00 sessions=1 origin_excluded=1 proposed=1 rejected=0 dropped_stale=0
    calls=1 provider=inherited status=ok'' - each carrying
    sessions/origin_excluded/proposed/rejected/dropped_stale/calls/provider/status, with
    `origin_excluded=1` beside `sessions=1` for the one session refused by origin, and
    `provider=inherited` naming which model was billed; a run that proposed nothing still reported,
    with status=ok; a line written before either field existed still parses (it simply lacks the
    keys), and `status` is still last so a degraded run''s own sentence cannot swallow a field; and
    the log leaves the store''s tree clean (excluded through .git/info/exclude, where the write lock
    lives)". The field the row was red for is present, in the place §9 puts it, carrying a real
    refusal rather than a zero any implementation would print. One-line-per-run, always, even at
    zero proposed, is unchanged and still Kept.

    '
  previous_disposition: GAP
- id: AMM-036
  title: suggestions.v2 Core 10 — Fail open
  contract:
    file: contracts/suggestions.v2.md
    clause: Core 10
    quote: Fail open.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/suggestions/run.py::probe_core_10
  work: amplifier_bundle_memory-b0g
  notes: 'Kept 2026-09-06 by lane P. probe_core_10 drives all three failure modes: a missing substrate
    is status=degraded:substrate missing with no model call and no inbox write, a model_call that raises
    is counted and named, and a reply that is not the structured shape is counted as rejected. Each left
    one log line, nothing reached the inbox, doctor''s substrate row is WARN (never FAIL), and the timer
    sees exit 0 in every case.


    2026-09-07 19:10Z — carried to suggestions.v2 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-041
  title: session.v4 Core 11 — Injection ceiling <= 500 tokens
  contract:
    file: contracts/session.v4.md
    clause: Core 11
    quote: What this bundle injects into every model request is bounded.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/budget/run.py::probe_core_11
  work: amplifier_bundle_memory-nyh
  notes: 'Seeded 2026-09-07 15:05Z at GAP with the measurement that motivated the clause (tiktoken cl100k,
    shipped bundle): tool DESCRIPTION 576 · INPUT_SCHEMA text 232 · four skill lines 150 · framing sentence
    53 = 1,011 tokens against a 500 cap; the MEMORY.md body (147) is excluded by the clause. Target after
    the diet: ~220 + ~110 + ~65 + ~35 = ~430. The probe prints the per-source breakdown and fails above
    500.


    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lane 13-A. `cd modules/tool-memory
    && uv run --offline python ../../conformance/session/budget/run.py` prints the per-source breakdown
    — DESCRIPTION 190 · INPUT_SCHEMA text 139 · skill memory 18 · skill remember 21 · framing 41 — and
    `session.v3 Core 11 — Kept — 409 of 500 tokens (91 spare)`. Was 1,011 on the v2 bundle. skill_lines()
    reads frontmatter via yaml.safe_load (the lane''s regex only read folded blocks; caught at the post-merge
    gate).


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-042
  title: session.v4 Conformance — nothing given to the model asserts what the human can see
  contract:
    file: contracts/session.v4.md
    clause: Conformance
    quote: Nothing the model is given asserts what the human can or cannot see of a tool call
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/budget/run.py::probe_no_presumption
  work: amplifier_bundle_memory-20e
  notes: 'Seeded 2026-09-07 15:05Z at GAP. Presuming phrases present today: `the tool result is what the
    human reads` (tool DESCRIPTION; all four skills), `Say nothing.` (all four skills), `it is counted,
    not read` (DESCRIPTION; tool-memory/README.md), `the human reads` in both module READMEs and in hooks-memory-inject
    docstrings. Evidence: the steward''s /memory listing of 2026-09-07 rendered exactly and reached nobody
    — the client hid the tool result.


    2026-09-07 16:05Z — Kept, wave 13 (the manager''s gate on main @ 6c64fb5): lanes 13-A/B/C. probe_no_presumption
    walks modules/, skills/, behaviors/, bundle.md (not contracts/, not itself), case-insensitive, and
    prints `probe_no_presumption — Kept — no presuming phrase in modules/, skills/, behaviors/, bundle.md`.
    The last hit (a test docstring, `The human reads nothing.`, tool-memory tests:951) was removed by
    the manager at the gate.


    2026-09-07 19:10Z — carried to session.v4 2026-09-07 (clause unchanged).

    '
  previous_disposition: GAP
- id: AMM-043
  title: 'store.v3 Core 11 — `enabled: false` makes the instance inert'
  contract:
    file: contracts/store.v3.md
    clause: Core 11
    quote: '`enabled: false` makes the instance inert.'
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_11
  work: amplifier_bundle_memory-6x6
  notes: 'Seeded 2026-09-07 19:10Z on the re-lock: Not yet — nothing of it exists. The clause: `Nothing
    injects, no memory tool is offered, no skills are advertised, and no timer runs against it. `doctor`
    names the instance as disabled rather than reporting it healthy.` Measured today: `grep -rn ''enabled''
    src/amplifier_memory/*.py` finds only the systemd `is-enabled` query (doctor.py:464-484, service.py:26)
    — no instance flag is read or written anywhere, there is no config.yaml to hold one (§2, AMM-002),
    and doctor has no disabled state. The four surfaces the clause names are owned by three lanes: the
    library predicate and the writer refusals by lane U (6x6), the hook/tool silence by lane V (8o4),
    the doctor line by lane W (l3e). probe_core_11 does not exist yet; it is named here as the probe the
    closing lane writes, the same way AMM-041 named its probe before conformance/session/budget existed.

    2026-09-07 19:30Z — CONFORMS by lane U (6x6), merged d324c6c; the manager re-ran conformance/store/run.py::probe_core_11 on main: Kept — enabled: false → store.save/edit/forget and inbox writers refuse with one line naming the instance as inert; nothing written.

    '
- id: AMM-044
  title: session.v4 Core 12 — Which instance a session uses is configuration
  contract:
    file: contracts/session.v4.md
    clause: Core 12
    quote: Which instance a session uses is configuration.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/inject/run.py::check_core_12
  work: amplifier_bundle_memory-8o4
  notes: 'Seeded 2026-09-07 19:10Z on the re-lock: Not yet. The clause: `Both session-plane modules accept
    in their mount-plan `config:` block a key `home: <path>` naming the instance (store §1) they read
    and write; absent, the store contract''s resolution order decides, so a session with no `home:` behaves
    exactly as today. Both honour that instance''s `config.yaml` `enabled` key.` and, when disabled, `refuses
    every operation with one line, `memory is disabled for this instance (<path>: enabled: false).`` Measured
    today: the hook reads exactly one key from its mount config, `priority` (modules/hooks-memory-inject/amplifier_module_hooks_memory_inject/__init__.py:295-296);
    the tool stores `self.config` and reads no key at all (modules/tool-memory/amplifier_module_tool_memory/__init__.py:560).
    Neither knows `home:` or `enabled`. The no-`home:`-behaves-as-today half is true by construction today
    and must stay true; it is the two keys that do not exist. Lane V (8o4). probe_core_12 is named here
    as the probe the closing lane writes.

    2026-09-07 20:15Z — CONFORMS by lane V (8o4), merged b9e9eef; the reconciler re-ran both session
    kits on merged main (6a64d7e), each in its own module venv.
    conformance/session/inject/run.py::check_core_12: Kept — "`config: home:
    /tmp/tmpebnkxmcj/c12-planned` read that instance while $AMPLIFIER_MEMORY_HOME named
    /tmp/tmpebnkxmcj/c12-other — the plan wins; enabled: false → 3 requests: nothing injected, no
    line rendered, no user_message, no error-log line, and neither sessions.jsonl nor usage.jsonl
    created — the session plane is silent, not failing; discriminating arm: the same config with
    enabled: true injects and says ''1 memory loaded from /tmp/tmpebnkxmcj/c12-live.''".
    conformance/session/tool/run.py::check_core_12: Kept — "`config: home:
    /tmp/tmp9for8m4a/c12-planned` listed that instance while $AMPLIFIER_MEMORY_HOME named
    /tmp/tmp9for8m4a/c12-other — the plan wins; save landed in the planned instance and nowhere else
    […] enabled: false → all 7 operations (save, edit, forget, list, overview, cite, review) refuse
    with one line, ''memory is disabled for this instance (/tmp/tmp9for8m4a/c12-inert: enabled:
    false).''; MEMORY.md unchanged; the tool''s description IS that line, so an inert instance
    advertises nothing to the model either; the refusal is byte-identical to the library''s own
    InstanceDisabled". Both modules now read `home:` and `enabled`, the refusal line is the
    clause''s own, and the no-`home:`-behaves-as-today half is measured, not assumed. LEDGER
    CORRECTION in the same edit: this row was seeded naming a probe `probe_core_12` that the closing
    lane never wrote — the kits call it `check_core_12`, in both session kits. The ref is moved to
    the function that exists and ran; no verdict rests on the correction. The tool kit''s arm is
    cited here rather than in the assertion field because a row carries one ref; both were run and
    both are quoted above.

    '
  previous_disposition: GAP
- id: AMM-045
  title: session.v4 Core 13 — Which sessions have a human in them
  contract:
    file: contracts/session.v4.md
    clause: Core 13
    quote: Which sessions have a human in them.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/session/inject/run.py::check_core_13
  work: amplifier_bundle_memory-8o4
  notes: 'Seeded 2026-09-07 19:10Z on the re-lock: Not yet — nothing of it exists. The clause: `A session
    declares its origin at start through one environment variable any launcher can export, `AMPLIFIER_SESSION_ORIGIN`,
    one of `human` · `worker` · `recipe` · `agent` · `eval`. Unset means `human`. At session start the
    inject hook appends `{session_id, origin, first_seen}` to the instance''s `sessions.jsonl`` and, for
    any other origin, `the tool refuses `save`, `edit` and `forget` exactly the way it refuses a sub-agent
    today (R2''s one-line refusal, naming the origin), the suggestions line ... is not rendered, and the
    suggest job treats the session as having no human interlocutor`. Measured today: `grep -rn ''AMPLIFIER_SESSION_ORIGIN|sessions.jsonl''
    src/ modules/` returns zero hits — the variable is read nowhere, sessions.jsonl is written nowhere,
    and the tool''s only write-refusal is R2''s parent_id test (AMM-030), which is a different question.
    The unset-means-human half is vacuously true today and must stay true after the change. Reads against
    store §2''s sessions.jsonl (AMM-002) and is the input suggestions.v2 Core 2 (AMM-038) needs. Lane
    V (8o4). probe_core_13 is named here as the probe the closing lane writes.

    2026-09-07 20:15Z — CONFORMS by lane V (8o4), merged b9e9eef; the reconciler re-ran both session
    kits on merged main (6a64d7e), each in its own module venv.
    conformance/session/inject/run.py::check_core_13: Kept — "unset $AMPLIFIER_SESSION_ORIGIN → one
    line {''session_id'': ''sess-human'', ''origin'': ''human'', ''first_seen'':
    ''2026-09-07T20:14:10.956055+00:00''} (unset means human) and the suggestions line still
    renders: ''3 suggestions waiting. /memory review to see them.''; origin=worker →
    {''session_id'': ''sess-worker'', ''origin'': ''worker'', ''first_seen'':
    ''2026-09-07T20:14:10.964696+00:00''}; §1''s block still injected (262 chars); the only line
    shown is the load line […] — no suggestions line for a session with nobody in it; the same
    session id across 4 requests and two mounts is still exactly one line (first_seen unmoved […]);
    the suggest job''s own reader agrees: session_origins() = {''sess-human'': ''human'',
    ''sess-worker'': ''worker''}". conformance/session/tool/run.py::check_core_13: Kept — "save
    refused before any library call: refused: session.v4 §13 — a worker session never saves; only a
    session with a human interlocutor may write. […] list allowed in a worker session — §13 forbids
    writing, not reading […] the same shape as R2''s, naming the origin instead […] discriminating
    arm: unset $AMPLIFIER_SESSION_ORIGIN saves as it always did […]; refused by name for every
    non-human origin: recipe, agent, eval". Every surface the clause names is measured: the variable
    read, sessions.jsonl appended once per session, the write refusal in R2''s shape naming the
    origin, the suggestions line withheld, and the job''s own reader agreeing — the input AMM-038
    needed. Also confirmed live by the manager at the gate, in the installed app (CHECK-RECORD 15b):
    AMPLIFIER_SESSION_ORIGIN=worker amplifier run → ~/.amplifier/memory/sessions.jsonl gained
    {"session_id": "ff73808a-…", "origin": "worker", "first_seen": "2026-09-07T20:06:46Z"}. LEDGER
    CORRECTION in the same edit: seeded naming `probe_core_13`, which the closing lane never wrote —
    the kits call it `check_core_13`; the ref is moved to the function that exists and ran, and no
    verdict rests on the correction.

    '
  previous_disposition: GAP
