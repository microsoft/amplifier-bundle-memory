# Conformance ledger — amplifier-bundle-memory. Top-level YAML list; row AMM-000 is SYNC.
# Dispositions: CONFORMS · GAP · VIOLATION · OPEN-PINNED · NOT-ASSERTABLE · EXCLUDED (read as
# Kept · Not yet · Broken · Pinned open · Can't check · excluded). Seeded by the manager session
# on 2026-09-06 from the four locked contracts; every row starts at the verdict the repository
# earns today. suggestions.v1 is (DRAFT) and has no rows until it is locked.
- id: AMM-000
  title: SYNC — pinned contract files
  contract:
    files:
      - path: contracts/store.v1.md
        sha256: cf78f9db4be910ce93af4fa7e54d8c076eba1dc520ebe7475f52c28942db5c65
      - path: contracts/session.v1.md
        sha256: e96684f7aca4cf9422690fa109acc67c23c223f0870f71101f35286fddcee700
      - path: contracts/cli.v1.md
        sha256: 5053f7f704214d4c1588f9df1fb43708d31dd52f1548e812a7e2b231d2e50637
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: ledger/checks/test_row_amm_000_sync.py::test_row_amm_000_sync
  work: amplifier_bundle_memory-3q5
  notes: >
    Probe written by lane A with the first conformance kit and passing: it re-hashes the three
    pinned files and fails loud on any drift. A hash mismatch still means a full-ledger
    re-review, never a silent bump.

- id: AMM-001
  title: store.v1 Core 1 — Location.
  contract:
    file: contracts/store.v1.md
    clause: "Core 1"
    quote: |
      Location.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_1
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. The probe asserts the store is a git repo at
    $AMPLIFIER_MEMORY_HOME and that init/save/forget are exactly 1/2/3 commits with a clean
    tree. Caveat recorded as a residual: log_usage also commits, so a per-request usage log
    would commit per request.

- id: AMM-002
  title: store.v1 Core 2 — Fixed layout.
  contract:
    file: contracts/store.v1.md
    clause: "Core 2"
    quote: |
      Fixed layout.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_2
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. The probe asserts the on-disk set is exactly MEMORY.md,
    declined.md, inbox.md, topics/, usage.jsonl, and that a stray notes.txt carrying an
    m-NNN line is not read as memory. topics/.gitkeep is a git mechanism, not memory.

- id: AMM-003
  title: store.v1 Core 4 — The cap is enforced by the writer, not by advice.
  contract:
    file: contracts/store.v1.md
    clause: "Core 4"
    quote: |
      The cap is enforced by the writer, not by advice.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_4
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. Discriminating pair: the 200th MEMORY.md line saves, the 201st
    raises CapExceeded naming 200, 'topic file' and '/forget', and the refused line is not
    written. Headings and blank lines count toward the cap.

- id: AMM-004
  title: store.v1 Core 5 — Topic files
  contract:
    file: contracts/store.v1.md
    clause: "Core 5"
    quote: |
      Topic files
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_5
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. The probe asserts a new topic file must begin with a one-line
    purpose, the 151st line of a topic file is refused, and the 51st topic file is refused
    and never created.

- id: AMM-005
  title: store.v1 Core 6 — Provenance lives in git.
  contract:
    file: contracts/store.v1.md
    clause: "Core 6"
    quote: |
      Provenance lives in git.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_6
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. Every writer commit carries [m-NNN], the text, quote: "<verbatim>",
    session: and writer:; why(id) parses all five back from git log --grep. No separate
    provenance store exists.

- id: AMM-006
  title: store.v1 Core 7 — declined.md
  contract:
    file: contracts/store.v1.md
    clause: "Core 7"
    quote: |
      declined.md
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_006
  work: amplifier_bundle_memory-3q5
  notes: >
    Still Not yet 2026-09-06. init creates declined.md, but Phase 1 has no decline path: the
    append and the exact-match re-proposal block belong to suggestions.v1, which is (DRAFT).
    conformance/store/run.py::probe_core_7 prints "Core 7 - Can't check" for exactly that reason.

- id: AMM-007
  title: store.v1 Core 8 — usage.jsonl
  contract:
    file: contracts/store.v1.md
    clause: "Core 8"
    quote: |
      usage.jsonl
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_8
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. log_usage appends {ts,event,target,session_id} and truncates to
    the last 90 days on each write; a seeded 91-day-old entry is dropped and nothing else is.

- id: AMM-008
  title: store.v1 Core 9 — Two writers, one path.
  contract:
    file: contracts/store.v1.md
    clause: "Core 9"
    quote: |
      Two writers, one path.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_9
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A. A hand-edited line is read back by list_memories, is never
    clobbered by the writer, and its id is never reissued; hand and writer commits are both
    ordinary git commits.

- id: AMM-009
  title: store.v1 Core 10 — Bounded by construction.
  contract:
    file: contracts/store.v1.md
    clause: "Core 10"
    quote: |
      Bounded by construction.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: conformance/store/run.py::probe_core_10
  work: amplifier_bundle_memory-3q5
  notes: >
    Kept 2026-09-06 by lane A for Phase 1: MEMORY.md 200, topic 150, topics 50 and usage 90d all
    refuse or truncate under probe. The inbox 30-day expiry is suggestions.v1 (DRAFT), outside
    Phase 1 and outside this probe.

- id: AMM-010
  title: session.v1 Core 1 — Loaded in every request.
  contract:
    file: contracts/session.v1.md
    clause: "Core 1"
    quote: |
      Loaded in every request.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_010
  work: amplifier_bundle_memory-zqi
  notes: >
    Kept 2026-09-06 by modules/hooks-memory-inject: one handler on provider:request
    returns inject_context / role=system / ephemeral=True carrying the §1 framing
    sentence as the first line inside the envelope, MEMORY.md verbatim, and no topic
    bodies; two instances over the same store are byte-identical and carry no
    timestamp or session id. Probe lives in the module suite: `cd modules/hooks-memory-inject && uv run --offline pytest -q -k test_row_amm_010` (18 passed 2026-09-06). It is NOT yet discovered by the repo-root `uv run pytest` — wiring the module suites into the root gate is a residual for the manager.
    conformance/session/inject/run.py additionally re-extracts the framing sentence
    from the locked contract and compares it byte-for-byte with the module constant,
    so the two cannot drift apart silently. Registration on provider:request (not
    session:start, not context:post_compact) is what makes "every request, including
    after a compaction" true by construction — no first-turn flag exists to go stale.

- id: AMM-011
  title: session.v1 Core 2 — Announce the load, once.
  contract:
    file: contracts/session.v1.md
    clause: "Core 2"
    quote: |
      Announce the load, once.
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: >
    Model behaviour (the announce line). Proven only by the real-host smoke (lane 16w, tests/smoke/), recorded in docs/workflow/CHECK-RECORD.md.
  work: amplifier_bundle_memory-zqi
  notes: >
    Still Can't check after 2026-09-06, deliberately. The injected block now ends with
    a static instruction asking for the one-time announce, and both variants are
    verified in-process (populated: 'Loaded N memories (M topics available).'; empty:
    'No memories yet — /remember <text> to add one.') by
    conformance/session/inject/run.py and the module suite. Whether the assistant
    actually *says* it once cannot be observed in this process; only the real-host
    smoke can close this row.

- id: AMM-012
  title: session.v1 Core 3 — Save on correction, in the same turn.
  contract:
    file: contracts/session.v1.md
    clause: "Core 3"
    quote: |
      Save on correction, in the same turn.
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: >
    Model behaviour (saving in the turn). Proven only by the real-host smoke's discriminating pair (lane 16w).
  work: amplifier_bundle_memory-yvu

- id: AMM-013
  title: session.v1 Core 4 — Do not save
  contract:
    file: contracts/session.v1.md
    clause: "Core 4"
    quote: |
      Do not save
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: >
    Model behaviour (not saving task-scoped instructions). Proven only by the real-host smoke (lane 16w).
  work: amplifier_bundle_memory-yvu

- id: AMM-014
  title: session.v1 Core 5 — The model proposes; the writer commits.
  contract:
    file: contracts/session.v1.md
    clause: "Core 5"
    quote: |
      The model proposes; the writer commits.
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_014
  work: amplifier_bundle_memory-yvu
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-015
  title: session.v1 Core 6 — `/remember <text>`
  contract:
    file: contracts/session.v1.md
    clause: "Core 6"
    quote: |
      `/remember <text>`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_015
  work: amplifier_bundle_memory-yvu
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-016
  title: session.v1 Core 7 — Recall is reading.
  contract:
    file: contracts/session.v1.md
    clause: "Core 7"
    quote: |
      Recall is reading.
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: >
    Model behaviour (reading a topic file and saying so). Proven only in a real session.
  work: amplifier_bundle_memory-yvu

- id: AMM-017
  title: session.v1 Core 8 — Cite at use.
  contract:
    file: contracts/session.v1.md
    clause: "Core 8"
    quote: |
      Cite at use.
  disposition: NOT-ASSERTABLE
  assertion:
    kind: none
  justification: >
    Model behaviour (citing the id at use). Proven only in a real session.
  work: amplifier_bundle_memory-yvu

- id: AMM-018
  title: session.v1 Core 9 — Nothing at session end.
  contract:
    file: contracts/session.v1.md
    clause: "Core 9"
    quote: |
      Nothing at session end.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_018
  work: amplifier_bundle_memory-zqi
  notes: >
    Kept 2026-09-06 for the inject hook only: the module registers exactly
    ['provider:request'] and its source contains no session:end / session_end /
    atexit. Probe lives in the module suite: `cd modules/hooks-memory-inject && uv run --offline pytest -q -k test_row_amm_018` (18 passed 2026-09-06). It is NOT yet discovered by the repo-root `uv run pytest` — wiring the module suites into the root gate is a residual for the manager.
    Scope caveat the manager should hold: this row is the whole bundle's clause, and
    only one of the bundle's modules exists so far. It must be re-checked when
    tool-memory and any Phase 2 timer land.

- id: AMM-019
  title: session.v1 Core 10 — Fail open, never block.
  contract:
    file: contracts/session.v1.md
    clause: "Core 10"
    quote: |
      Fail open, never block.
  disposition: CONFORMS
  assertion:
    kind: probe
    ref: test_row_amm_019
  work: amplifier_bundle_memory-zqi
  notes: >
    Kept 2026-09-06 for the inject hook only: store absent, MEMORY.md unreadable
    (chmod 000), and an unwritable error log each return action=continue with no
    injection and no raise; one line lands in
    ${AMPLIFIER_MEMORY_ERROR_LOG:-~/.amplifier/memory-errors.log}, deduplicated per
    reason per session. Probe lives in the module suite: `cd modules/hooks-memory-inject && uv run --offline pytest -q -k test_row_amm_019` (18 passed 2026-09-06). It is NOT yet discovered by the repo-root `uv run pytest` — wiring the module suites into the root gate is a residual for the manager.
    Same scope caveat as AMM-018: the writer and CLI have their own fail-open duty
    under this clause and do not exist yet.

- id: AMM-020
  title: cli.v1 Core 1 — Verbs.
  contract:
    file: contracts/cli.v1.md
    clause: "Core 1"
    quote: |
      Verbs.
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_020
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-021
  title: cli.v1 Core 2 — `status`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 2"
    quote: |
      `status`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_021
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-022
  title: cli.v1 Core 3 — `why <id>`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 3"
    quote: |
      `why <id>`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_022
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-023
  title: cli.v1 Core 4 — `review`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 4"
    quote: |
      `review`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_023
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-024
  title: cli.v1 Core 5 — `doctor`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 5"
    quote: |
      `doctor`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_024
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-025
  title: cli.v1 Core 6 — `service`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 6"
    quote: |
      `service`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_025
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-026
  title: cli.v1 Core 7 — `update`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 7"
    quote: |
      `update`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_026
  work: amplifier_bundle_memory-16w
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-027
  title: cli.v1 Core 8 — `init`
  contract:
    file: contracts/cli.v1.md
    clause: "Core 8"
    quote: |
      `init`
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_027
  work: amplifier_bundle_memory-3q5
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.

- id: AMM-028
  title: cli.v1 Core 9 — Thin wrapper.
  contract:
    file: contracts/cli.v1.md
    clause: "Core 9"
    quote: |
      Thin wrapper.
  disposition: GAP
  assertion:
    kind: probe
    ref: test_row_amm_028
  work: amplifier_bundle_memory-5hg
  notes: >
    Seeded 2026-09-06 with no code in the repository. Not yet.
