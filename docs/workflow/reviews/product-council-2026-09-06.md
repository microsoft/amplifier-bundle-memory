# Product council — 2026-09-06 (summary; full transcript in session 727da7ac…)

Roster: outcomist (front gate, CONCERN + unsoftened FAIL on the gate), intent-keeper (CONCERN),
user-advocate (FAIL, held), outcome-cartographer (CONCERN), positioning-critic (CONCERN),
bet-sizer (CONCERN). Two rounds; zero verdict changes; three standing disagreements.

**Blocker (user-advocate, held):** "A memory system whose first instruction to the human is
'I can't write these for you — you type them' has inverted its entire reason for existing."
Clears "the first time a real session shows a loaded style memory visibly changing a reply and
saying which memory did it."

**Second blocker (outcomist, bet-sizer):** the day-7 gate returns 2 even if the product works
perfectly all week; P(gate ≥5) ≈ 25%, P(reading means what it will be taken to mean) < 50%.

**Standing disagreements for the steward:** D1 principle 3 hints vs instructions (3–2–1; proposer
attached a falsifier — measure citations, decide day 7); D2 `edit` now vs after day 7 (lenses
crossed; free `$EDITOR` path either way); D3 pre-register principle 9's counting rule now
(as a note) vs amend after the reading.

**Per question:** Q1 no confirm gate 6/6; batch-save receipt "Saved 3 memories — my wording,
your call"; delete the false "I can't write these for you" line (5/6 named it the worst line).
Q2 render the announce deterministically (5/6); empty-store first contact carries the
differentiator ("travels to every session and project on this device"); drop `(0 topics
available)` and `(Phase 1 records none)`. Q3 `edit` yes 6/6, timing disputed; surface
`$EDITOR ~/.amplifier/memory/MEMORY.md` now (store.v1 §9). Q4 `/memory` should be free; hook
spike with pre-registered pass/fail; registry request filed, no dependency. Q5 delete sha and
phase; forget must echo its text (unanimous, safety). Q6 one-line failures (§10 already says
so); never: a receipt the disk contradicts, raw exceptions, a postmortem in chat, the model
repairing the store with bash. Q7 ids only, holes stay, bare N = m-00N (settled by fact). Q8
missing: a write path to topic files (highest value), cite-at-use as instrument, the baseline
(expires), the positioning sentence ("Memory isn't a replacement for the ruleset. It's where
your deltas live").

**Sequenced plan:** today — baseline, gate definition, skill text, ids rule, one-line failures,
`(my wording)` provenance, `$EDITOR` and topic-file paths, citation counting, registry email;
one-hour hook spike; the writer fix (landed as wave 4); days 2–6 real use, no candidate
reviews; day 7 one amendment cycle.
