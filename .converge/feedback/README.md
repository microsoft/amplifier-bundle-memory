# Feedback inbox

Drop anything here — a sentence, a pasted log, a screenshot path, a transcript.
One file per drop, any name (a date and a few words is enough). The manager
session reads it against the contracts and the queue and records what became
of it in the plan record. Drops are never edited or deleted.
